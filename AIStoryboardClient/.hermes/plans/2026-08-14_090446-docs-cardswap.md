# Docs 页面 CardSwap 组件集成 + 能力速览带 实现计划

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** 在 /docs 页「登录后即可开始 · 无需额外配置」与「界面总览」之间插入一个 `CardSwap`（React Bits 3D 卡片轮换堆叠）能力速览带，自动轮播展示 AI 分镜的核心能力。

**Architecture:** 纯前端、单页改动。落地 React Bits `CardSwap` 组件（gsap 驱动，已装），在 Hero 与「界面总览」之间加一个分节（左文字 + 右卡片堆叠），卡片内容用现有设计 tokens（cream/coral）。

**Tech Stack:** React 19 + TypeScript（verbatimModuleSyntax 开启）+ gsap 3.15（已装，CardSwap 只依赖 gsap，无需新装）+ 现有 `--color-*` / `--rounded-*` 设计 tokens。

---

## 现状与约束（已核实）

- 文件：`AIStoryboardClient/src/pages/DocsPage.tsx`；Hero 结尾是 `<div className="docs-hero__hint">登录后即可开始 · 无需额外配置</div>`，紧接着 `</header>`，之后是 `{/* 界面总览 */}` 分节。
- 样式：`AIStoryboardClient/src/styles/docs.css`（已有 `.docs-split` 左右分栏、`.docs-section`、`.docs-point` 等）。
- tsconfig 开启 `verbatimModuleSyntax`：类型必须 `import type` 或内联 `type` 修饰符（SpecularButton / TextType 均已踩过此坑并解决）。
- `gsap` 已在依赖中；CardSwap 的 `.card-swap-container` 默认 `position:absolute; bottom:0; right:0; translate(5%,20%)` —— 需放在相对定位容器内，天然适配「右侧视觉 + 左侧文字」的分栏。

---

## Task 1: 创建 CardSwap 组件（适配 verbatimModuleSyntax）

**Objective:** 落地 React Bits `CardSwap`，把 import 拆成值/类型两部分，避免 TS1484 报错。

**Files:**
- Create: `AIStoryboardClient/src/components/CardSwap.tsx`

**Step 1: 写完整组件（已适配，可直接粘贴）**

```tsx
import * as React from 'react';
import { Children, cloneElement, forwardRef, isValidElement, useEffect, useMemo, useRef } from 'react';
import type { ReactElement, ReactNode, RefObject } from 'react';
import gsap from 'gsap';
import './CardSwap.css';

export interface CardSwapProps {
  width?: number | string;
  height?: number | string;
  cardDistance?: number;
  verticalDistance?: number;
  delay?: number;
  pauseOnHover?: boolean;
  onCardClick?: (idx: number) => void;
  skewAmount?: number;
  easing?: 'linear' | 'elastic';
  children: ReactNode;
}

export interface CardProps extends React.HTMLAttributes<HTMLDivElement> {
  customClass?: string;
}

export const Card = forwardRef<HTMLDivElement, CardProps>(({ customClass, ...rest }, ref) => (
  <div ref={ref} {...rest} className={`card ${customClass ?? ''} ${rest.className ?? ''}`.trim()} />
));
Card.displayName = 'Card';

type CardRef = RefObject<HTMLDivElement | null>;
interface Slot {
  x: number;
  y: number;
  z: number;
  zIndex: number;
}

const makeSlot = (i: number, distX: number, distY: number, total: number): Slot => ({
  x: i * distX,
  y: -i * distY,
  z: -i * distX * 1.5,
  zIndex: total - i,
});

const placeNow = (el: HTMLElement, slot: Slot, skew: number) =>
  gsap.set(el, {
    x: slot.x,
    y: slot.y,
    z: slot.z,
    xPercent: -50,
    yPercent: -50,
    skewY: skew,
    transformOrigin: 'center center',
    zIndex: slot.zIndex,
    force3D: true,
  });

const CardSwap: React.FC<CardSwapProps> = ({
  width = 500,
  height = 400,
  cardDistance = 60,
  verticalDistance = 70,
  delay = 5000,
  pauseOnHover = false,
  onCardClick,
  skewAmount = 6,
  easing = 'elastic',
  children,
}) => {
  const config =
    easing === 'elastic'
      ? {
          ease: 'elastic.out(0.6,0.9)',
          durDrop: 2,
          durMove: 2,
          durReturn: 2,
          promoteOverlap: 0.9,
          returnDelay: 0.05,
        }
      : {
          ease: 'power1.inOut',
          durDrop: 0.8,
          durMove: 0.8,
          durReturn: 0.8,
          promoteOverlap: 0.45,
          returnDelay: 0.2,
        };

  const childArr = useMemo(() => Children.toArray(children) as ReactElement<CardProps>[], [children]);
  const refs = useMemo<CardRef[]>(() => childArr.map(() => React.createRef<HTMLDivElement>()), [childArr.length]);

  const order = useRef<number[]>(Array.from({ length: childArr.length }, (_, i) => i));

  const tlRef = useRef<gsap.core.Timeline | null>(null);
  const intervalRef = useRef<number>(0);
  const container = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const total = refs.length;
    refs.forEach((r, i) => placeNow(r.current!, makeSlot(i, cardDistance, verticalDistance, total), skewAmount));

    const swap = () => {
      if (order.current.length < 2) return;

      const [front, ...rest] = order.current;
      const elFront = refs[front].current!;
      const tl = gsap.timeline();
      tlRef.current = tl;

      tl.to(elFront, { y: '+=500', duration: config.durDrop, ease: config.ease });

      tl.addLabel('promote', `-=${config.durDrop * config.promoteOverlap}`);
      rest.forEach((idx, i) => {
        const el = refs[idx].current!;
        const slot = makeSlot(i, cardDistance, verticalDistance, refs.length);
        tl.set(el, { zIndex: slot.zIndex }, 'promote');
        tl.to(
          el,
          { x: slot.x, y: slot.y, z: slot.z, duration: config.durMove, ease: config.ease },
          `promote+=${i * 0.15}`,
        );
      });

      const backSlot = makeSlot(refs.length - 1, cardDistance, verticalDistance, refs.length);
      tl.addLabel('return', `promote+=${config.durMove * config.returnDelay}`);
      tl.call(() => { gsap.set(elFront, { zIndex: backSlot.zIndex }); }, undefined, 'return');
      tl.to(elFront, { x: backSlot.x, y: backSlot.y, z: backSlot.z, duration: config.durReturn, ease: config.ease }, 'return');

      tl.call(() => { order.current = [...rest, front]; });
    };

    swap();
    intervalRef.current = window.setInterval(swap, delay);

    if (pauseOnHover) {
      const node = container.current!;
      const pause = () => { tlRef.current?.pause(); clearInterval(intervalRef.current); };
      const resume = () => { tlRef.current?.play(); intervalRef.current = window.setInterval(swap, delay); };
      node.addEventListener('mouseenter', pause);
      node.addEventListener('mouseleave', resume);
      return () => {
        node.removeEventListener('mouseenter', pause);
        node.removeEventListener('mouseleave', resume);
        clearInterval(intervalRef.current);
      };
    }
    return () => clearInterval(intervalRef.current);
  }, [cardDistance, verticalDistance, delay, pauseOnHover, skewAmount, easing]);

  const rendered = childArr.map((child, i) =>
    isValidElement<CardProps>(child)
      ? cloneElement(child, {
          key: i,
          ref: refs[i],
          style: { width, height, ...(child.props.style ?? {}) },
          onClick: (e) => {
            child.props.onClick?.(e as React.MouseEvent<HTMLDivElement>);
            onCardClick?.(i);
          },
        } as CardProps & React.RefAttributes<HTMLDivElement>)
      : child,
  );

  return (
    <div ref={container} className="card-swap-container" style={{ width, height }}>
      {rendered}
    </div>
  );
};

export default CardSwap;
```

**Step 2: 验证** — `cd AIStoryboardClient && npx tsc -p tsconfig.app.json --noEmit`，预期 0 错误（若报类型导入错误，确认 `import type { ReactElement, ReactNode, RefObject }` 就位）。

---

## Task 2: 创建 CardSwap 样式（适配设计规范）

**Objective:** 落地 CSS，把 `.card` 的默认黑底白边改为项目 cream/coral 设计 tokens。

**Files:**
- Create: `AIStoryboardClient/src/components/CardSwap.css`

**Step 1: 写完整 CSS（已适配颜色）**

```css
.card-swap-container {
  position: absolute;
  bottom: 0;
  right: 0;
  transform: translate(5%, 20%);
  transform-origin: bottom right;

  perspective: 900px;
  overflow: visible;
}

.card {
  position: absolute;
  top: 50%;
  left: 50%;
  border-radius: 12px;
  border: 1px solid var(--color-hairline, #e6dfd8);
  background: var(--color-surface-card, #efe9de);
  box-shadow: 0 16px 40px rgba(20, 20, 19, 0.18);

  transform-style: preserve-3d;
  will-change: transform;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
}

@media (max-width: 768px) {
  .card-swap-container {
    transform: scale(0.75) translate(25%, 25%);
  }
}

@media (max-width: 480px) {
  .card-swap-container {
    transform: scale(0.55) translate(25%, 25%);
  }
}
```

> 卡片内部内容（emoji + 标题 + 描述）由 `<Card>` 子元素用内联样式绘制，基类只负责 cream 底 + hairline 边 + 阴影。

---

## Task 3: 在 DocsPage 插入 CardSwap 能力速览带

**Objective:** 在 Hero hint 与「界面总览」之间插入一个分节：左侧标题+要点，右侧 CardSwap 卡片堆叠（自动轮播核心能力）。

**Files:**
- Modify: `AIStoryboardClient/src/pages/DocsPage.tsx`

**Step 1: 引入组件**

```tsx
import CardSwap, { Card } from '../components/CardSwap';
```

**Step 2: 新增「核心能力」数据（放在 `/* ── 数据 ── */` 区，紧邻 PARAMS 前）**

```tsx
const FEATURES = [
  { icon: '🎞️', title: 'AI 分镜', desc: '剧本一键拆解成分镜镜头' },
  { icon: '🖼️', title: '图片生成', desc: '文生图 / 图生图 / 参考图改图' },
  { icon: '🎬', title: '视频生成', desc: '文生视频 / 图生视频（参考图作首帧）' },
  { icon: '☾', title: 'Moon 智能体', desc: '对话式创作，计划模式 + 人工确认' },
  { icon: '📎', title: '图片理解', desc: '上传参考图，理解风格再生成' },
];
```

**Step 3: 在 `</header>` 之后、「界面总览」分节之前插入分节**

```tsx
      {/* 核心能力速览（CardSwap 3D 卡片轮换） */}
      <section className="docs-section docs-section--alt">
        <div className="docs-container" data-reveal>
          <div className="docs-split docs-split--wide-left" style={{ alignItems: 'center' }}>
            <div>
              <div className="docs-section__eyebrow">核心能力</div>
              <h2 className="docs-section__title">一块画布，五种能力</h2>
              <p className="docs-section__lead">
                从剧本拆解分镜，到生成图片与视频，再到 Moon 智能体对话式创作——核心能力全在这一套工作流里。
              </p>
              <div className="docs-points" style={{ marginTop: 'var(--space-lg)' }}>
                {FEATURES.map((f) => (
                  <div className="docs-point" key={f.title}>
                    <div className="docs-point__num">{f.icon}</div>
                    <div>
                      <div className="docs-point__name">{f.title}</div>
                      <div className="docs-point__desc">{f.desc}</div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
            <div style={{ height: 460, position: 'relative' }}>
              <CardSwap width={320} height={400} cardDistance={40} verticalDistance={46} delay={3000} skewAmount={4}>
                {FEATURES.map((f) => (
                  <Card key={f.title} style={{ padding: 28, display: 'flex', flexDirection: 'column', justifyContent: 'center' }}>
                    <div style={{ fontSize: 44, marginBottom: 16 }}>{f.icon}</div>
                    <div style={{ font: 'var(--font-display)', fontSize: 24, fontWeight: 400, color: 'var(--color-ink)', marginBottom: 8 }}>{f.title}</div>
                    <div style={{ fontSize: 13, color: 'var(--color-muted)', lineHeight: 1.6 }}>{f.desc}</div>
                  </Card>
                ))}
              </CardSwap>
            </div>
          </div>
        </div>
      </section>
```

**Step 4: 验证** — tsc + build 通过；浏览器 `/docs` 滚动到 Hero 下方，右侧卡片自动 3D 轮换。

---

## 验证清单（最终）

```bash
cd /e/Desktop/AI-storyboard/AIStoryboardClient
npx tsc -p tsconfig.app.json --noEmit   # 预期 0 错误
npm run build                            # 预期 ✓ built, exit 0
```

浏览器 `http://localhost:5173/docs` 手测：
1. 「登录后即可开始 · 无需额外配置」下方出现「核心能力」分节。
2. 右侧 5 张卡片 3D 堆叠，每 3 秒自动轮换（顶部卡片下沉、其余推进）。
3. 卡片 cream 底 + hairline 边 + 珊瑚点缀，符合设计规范。
4. 移动端（<768px）卡片自动缩放，不溢出。
5. `prefers-reduced-motion` 下无冲突（CardSwap 的 gsap 不依赖本项目 reduced-motion 分支，但建议保持 pauseOnHover=false 默认，避免额外交互负担）。

---

## 风险与取舍

- **风险 1：`verbatimModuleSyntax` 类型导入** → Task 1 已用 `import * as React` + `import type { ReactElement, ReactNode, RefObject }`，覆盖所有 `React.*` 类型与值引用。
- **风险 2：CardSwap 依赖 `Children.toArray` + refs，若 `<Card>` 数量变化会重建 refs** → 本用例卡片数量固定（5），无动态增删，安全。
- **风险 3：`.card-swap-container` 默认绝对定位 bottom-right** → Task 3 用相对定位的固定高度容器包裹，卡片落在容器右下，与左侧文字分栏对齐。
- **风险 4：CardSwap 的 gsap 循环动画与页面 `[data-reveal]` 淡入无冲突**（作用在不同元素），无需额外处理。
- **取舍**：卡片内容用静态 `FEATURES` 数据（5 项），不做可点击跳转（`onCardClick` 不接），避免文档页引入交互复杂度（YAGNI）。

## 待确认（open questions）

1. CardSwap 卡片是否需要点击跳转到对应文档段落（`onCardClick` → 锚点滚动）？计划默认不做（仅展示）。
2. 卡片尺寸/间距是否合适（width 320 / height 400 / delay 3s）？如需更大更慢可在 Task 3 直接调参。
