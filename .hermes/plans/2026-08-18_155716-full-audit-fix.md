# AI Storyboard 全维度审计修复计划

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** 修复全维度审计发现的 49 项问题（8 高 / 29 中 / 12 低），覆盖安全、性能、故障处置、异常补全、日志、配置、监控、冷数据、接口、边界、技术债、体验 12 个维度。

**Architecture:** 按优先级分 12 个 Phase，每 Phase 若干 Task（2-5 min/个）。后端 Spring Boot 4 + MyBatis-Plus，网关独立 Spring Boot，前端 React/TS/Zustand。

**Tech Stack:** Spring Boot 4, JDK 21, MyBatis-Plus 3.5.16, PostgreSQL, Spring AI 2.0, React 19, TypeScript, Zustand 5, Vite 8

**验证命令:**
```bash
# 后端编译
export JAVA_HOME="C:\\Program Files\\Java\\jdk-21"
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q

# 网关编译
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AILLMGateway\\pom.xml" compile -q

# 前端类型检查 + 构建
cd AIStoryboardClient && npx tsc -p tsconfig.app.json --noEmit && npm run build
```

---

## Phase 1: 安全优化（S1 路径穿越 + S4 输入校验）

### Task 1.1: [S1] FileController 路径穿越修复

**Objective:** 防止 `../../etc/passwd` 式路径穿越读取任意文件

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/impl/FileStorageServiceImpl.java:235-247`

**Step 1: 在 resolveImage/resolveVideo/resolveAudio 三个方法中加入路径校验**

在 `FileStorageServiceImpl.java` 中，三个 resolve 方法改为：

```java
@Override
public Path resolveImage(String filename) {
    return safeResolve(IMAGES_DIR, filename);
}

@Override
public Path resolveVideo(String filename) {
    return safeResolve(VIDEOS_DIR, filename);
}

@Override
public Path resolveAudio(String filename) {
    return safeResolve(AUDIOS_DIR, filename);
}

/** 路径穿越防护：resolve + normalize 后校验必须在目标目录下 */
private Path safeResolve(Path dir, String filename) {
    Path resolved = dir.resolve(filename).normalize();
    if (!resolved.startsWith(dir.normalize())) {
        throw new BusinessException(40001, "非法文件路径");
    }
    return resolved;
}
```

需要在文件头部加 import:
```java
import com.storyboard.exception.BusinessException;
```

**Step 2: 验证编译**

```bash
export JAVA_HOME="C:\\Program Files\\Java\\jdk-21"
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q
```

Expected: BUILD SUCCESS

**Step 3: Commit**

```bash
cd /e/Desktop/AI-storyboard
git add AIStoryboardBackend/src/main/java/com/storyboard/service/impl/FileStorageServiceImpl.java
git commit -m "fix(security): 防止 FileController 路径穿越读取任意文件"
```

- [ ] 完成

---

### Task 1.2: [S4] 关键 Controller 入参加 @Valid 校验

**Objective:** AgentSendMessageRequest 等 record 加校验注解，Controller 加 @Valid

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/dto/request/AgentSendMessageRequest.java`
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/dto/request/AgentCreateConversationRequest.java`
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/controller/AgentConversationController.java`

**Step 1: 给 record 加校验注解**

先读取现有 record 定义确认字段：
```bash
cat AIStoryboardBackend/src/main/java/com/storyboard/dto/request/AgentSendMessageRequest.java
cat AIStoryboardBackend/src/main/java/com/storyboard/dto/request/AgentCreateConversationRequest.java
```

给 `AgentSendMessageRequest.content` 加 `@NotBlank`，`AgentCreateConversationRequest.projectId` 加 `@NotBlank`。
Controller 对应方法参数加 `@Valid`。

**Step 2: 验证编译**

```bash
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q
```

**Step 3: Commit**

```bash
git add AIStoryboardBackend/src/main/java/com/storyboard/dto/request/ AIStoryboardBackend/src/main/java/com/storyboard/controller/AgentConversationController.java
git commit -m "fix(security): Agent 对话接口入参加 @Valid 校验"
```

- [ ] 完成

---

## Phase 2: 性能与并发优化（P1-P8）

### Task 2.1: [P1] ProjectServiceImpl.toResponse 批量 SQL 替代逐行 UPDATE

**Objective:** 消除项目列表查询时的 N+1 UPDATE

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/impl/ProjectServiceImpl.java:100-115`

**Step 1: 用单条批量 UPDATE 替代循环**

在 `toResponse` 方法开头，将循环检查改为：

```java
private ProjectResponse toResponse(Project project) {
    // 批量重置卡在 generating 超过 5 分钟的场景（替代逐行检查+updateById）
    sceneMapper.resetStaleGenerating(project.getId());
    List<Scene> scenes = sceneMapper.findByProjectIdOrdered(project.getId());
    // ... 后续不变
}
```

在 `SceneMapper` 接口加方法：
```java
/** 批量重置指定项目中卡在 generating 超过 5 分钟的场景状态为 failed */
void resetStaleGenerating(@Param("projectId") String projectId);
```

在 `SceneMapper.xml` 加 SQL：
```xml
<update id="resetStaleGenerating">
    UPDATE scenes
    SET image_status = CASE WHEN image_status = 'generating' THEN 'failed' ELSE image_status END,
        video_status = CASE WHEN video_status = 'generating' THEN 'failed' ELSE video_status END,
        updated_at = NOW()
    WHERE project_id = #{projectId}
      AND (image_status = 'generating' OR video_status = 'generating')
      AND updated_at &lt; NOW() - INTERVAL '5 minutes'
</update>
```

**Step 2: 验证编译**

```bash
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q
```

**Step 3: Commit**

```bash
git add AIStoryboardBackend/src/main/java/com/storyboard/service/impl/ProjectServiceImpl.java
git add AIStoryboardBackend/src/main/java/com/storyboard/mapper/SceneMapper.java
# 找到对应的 xml 文件并 add
git commit -m "perf: ProjectServiceImpl.toResponse 批量 SQL 重置 generating 状态"
```

- [ ] 完成

---

### Task 2.2: [P2] GatewayModelServiceImpl 缓存踩踏修复

**Objective:** 用 computeIfAbsent 替代 put，防并发回填

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/ai/impl/GatewayModelServiceImpl.java:129-143`

**Step 1: 改 getDefaultModel 方法**

```java
private String getDefaultModel(String type) {
    return defaultCache.computeIfAbsent(type, t -> {
        String fetched = fetchDefaultModel(t);
        if (fetched != null) return fetched;
        // text 类型兜底从配置文件读取
        if ("text".equals(t)) return config.getGateway().getFallbackTextModel();
        return FALLBACK_DEFAULTS.getOrDefault(t, "");
    });
}
```

同时删除 `warmDefaultModels` 中的循环 put（computeIfAbsent 已覆盖）：
```java
@EventListener(ApplicationReadyEvent.class)
public void warmDefaultModels() {
    // 预热各类型默认模型进缓存（computeIfAbsent 保证并发安全）
    for (String type : FALLBACK_DEFAULTS.keySet()) {
        getDefaultModel(type);
    }
    getDefaultModel("text");
}
```

**Step 2: 验证编译**

```bash
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q
```

**Step 3: Commit**

```bash
git add AIStoryboardBackend/src/main/java/com/storyboard/service/ai/impl/GatewayModelServiceImpl.java
git commit -m "fix(perf): GatewayModelServiceImpl 缓存 computeIfAbsent 防踩踏"
```

- [ ] 完成

---

### Task 2.3: [P3] SysConfigServiceImpl @PostConstruct N+1 → 单次查询

**Objective:** 3 次 selectOne → 1 次 selectList

**Files:**
- Modify: `AILLMGateway/src/main/java/com/llmgateway/service/impl/SysConfigServiceImpl.java:50-63`

**Step 1: 改 loadIntoGatewayConfig**

```java
@PostConstruct
void loadIntoGatewayConfig() {
    Map<String, SysConfig> rows = sysConfigMapper.selectList(null).stream()
            .collect(java.util.stream.Collectors.toMap(SysConfig::getConfigKey, r -> r, (a, b) -> a));
    SPECS.forEach((key, spec) -> {
        SysConfig row = rows.get(key);
        if (row == null) return;
        try {
            spec.binder().accept(gatewayConfig, spec.parser().apply(row.getConfigValue().trim()));
        } catch (Exception e) {
            log.warn("系统配置加载失败，保留默认值: key={}, value={}, error={}", key, row.getConfigValue(), e.getMessage());
        }
    });
}
```

**Step 2: 验证编译**

```bash
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AILLMGateway\\pom.xml" compile -q
```

**Step 3: Commit**

```bash
git add AILLMGateway/src/main/java/com/llmgateway/service/impl/SysConfigServiceImpl.java
git commit -m "perf: SysConfigServiceImpl @PostConstruct N+1 → 单次查询"
```

- [ ] 完成

---

### Task 2.4: [P4] ConversationLock 内存泄漏——会话结束时清理 Semaphore

**Objective:** 防止 Semaphore Map 无限增长

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/agent/ConversationLock.java`

**Step 1: 加 releaseAndRemove 方法**

```java
/** 释放锁并移除条目（会话结束/删除时调用） */
public void releaseAndRemove(String conversationId) {
    Semaphore semaphore = locks.remove(conversationId);
    if (semaphore != null) {
        semaphore.release();
    }
}
```

在 `AgentChatServiceImpl.deleteConversation` 和 `clearMessages` 中调用 `conversationLock.releaseAndRemove(conversationId)`。

**Step 2: 验证编译**

```bash
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q
```

**Step 3: Commit**

- [ ] 完成

---

### Task 2.5: [P5] AisplitIntentHandler / AgentOrchestratorSupport 内存 Map 清理

**Objective:** regenCount / clarifyCount / sessionAssets 会话结束时清理

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/agent/handler/AisplitIntentHandler.java`
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/agent/handler/AgentOrchestratorSupport.java`

**Step 1: 加清理方法**

在 AisplitIntentHandler 加：
```java
/** 会话结束时清理内存态（regenCount / sessionAssets） */
public void cleanup(String conversationId) {
    regenCount.remove(conversationId);
    sessionAssets.remove(conversationId);
}
```

在 AgentOrchestratorSupport 加：
```java
/** 会话结束时清理内存态（clarifyCount） */
public void cleanupClarify(String conversationId) {
    clarifyCount.remove(conversationId);
}
```

在 `AgentChatServiceImpl.deleteConversation` 中调用对应清理。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 2.6: [P6] AgentChatServiceImpl 内存 Map 清理

**Objective:** renamedTitleByConversation / lastPicUrlByConversation / titleScheduled 会话结束时清理

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/agent/impl/AgentChatServiceImpl.java`

**Step 1: 在 deleteConversation 方法中加清理**

```java
@Override
@Transactional
public void deleteConversation(String userId, String conversationId) {
    AgentConversation conversation = getOwnedConversation(userId, conversationId);
    // 清理内存态
    titleScheduled.remove(conversationId);
    renamedTitleByConversation.remove(conversationId);
    lastPicUrlByConversation.remove(conversationId);
    conversationMapper.deleteById(conversationId);
}
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 2.7: [P7] MinimaxVideoServiceImpl / VideoGenerationServiceImpl firstSucceededAt 清理

**Objective:** 视频任务完成后清理 giveUp 计时 Map

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/ai/impl/MinimaxVideoServiceImpl.java`
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/ai/impl/VideoGenerationServiceImpl.java`

**Step 1: 在任务终态（completed/failed）后 remove**

在两个文件的 pollVideoTask 方法中，状态为 completed 或 failed 时加：
```java
firstSucceededAt.remove(taskId);
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 2.8: [P8] 前端 generatingVideo 轮询硬上限

**Objective:** 与 imageResumeTimer 一致，加 10 分钟 timeout

**Files:**
- Modify: `AIStoryboardClient/src/stores/projectStore.ts`

**Step 1: 找到 generatingVideo 轮询逻辑，加 setTimeout 10 分钟清除**

在 `resumeVideoGeneration` 函数中（约 L335 附近），加类似 imageResumeTimer 的硬上限：
```typescript
// ponytail: 10 分钟硬上限，防后端异常导致 videoStatus 卡 generating 时无限轮询
setTimeout(() => {
  if (!videoResumeTimer) return;
  clearInterval(videoResumeTimer);
  videoResumeTimer = null;
  set((s) => {
    const updatedScenes = s.scenes.map(sc => {
      if (sc.videoStatus === 'generating') {
        return { ...sc, videoStatus: 'failed' as const };
      }
      return sc;
    });
    return { scenes: updatedScenes };
  });
}, 600_000);
```

**Step 2: 验证前端构建**

```bash
cd AIStoryboardClient && npx tsc -p tsconfig.app.json --noEmit && npm run build
```

**Step 3: Commit**

- [ ] 完成

---

## Phase 3: 故障处置能力（F1-F5）

### Task 3.1: [F1] FileController FileInputStream 句柄泄漏修复

**Objective:** 用 try-with-resources 防止异常时文件句柄泄漏

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/controller/FileController.java:35-83`

**Step 1: 三个 get 方法改为 try-with-resources**

以 getImage 为例：
```java
@GetMapping("/images/{filename}")
public ResponseEntity<Resource> getImage(@PathVariable String filename) throws IOException {
    Path filePath = fileStorageService.resolveImage(filename);
    if (!Files.exists(filePath)) {
        return ResponseEntity.notFound().build();
    }
    var contentType = MediaType.parseMediaType(FileStorageService.contentType(filename));
    return ResponseEntity.ok()
            .contentType(contentType)
            .contentLength(Files.size(filePath))
            .body(new InputStreamResource(Files.newInputStream(filePath)));
}
```

三个方法同样改法（用 `Files.newInputStream` 替代 `new FileInputStream`，Spring 自动管理 InputStreamResource 的生命周期）。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 3.2: [F2] FileStorageServiceImpl 下载先写 .tmp 再 rename

**Objective:** 防止下载中断留下不完整文件

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/impl/FileStorageServiceImpl.java:177-208`

**Step 1: downloadAndSave 改为先写 .tmp 再 rename**

```java
Path target = targetDir.resolve(filename);
Path tmp = targetDir.resolve(filename + ".tmp");
try (InputStream in = resp.body()) {
    Files.copy(in, tmp, StandardCopyOption.REPLACE_EXISTING);
}
Files.move(tmp, target, StandardCopyOption.ATOMIC_MOVE);
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 3.3: [F3] 网关 UpstreamClient 重试指数退避

**Objective:** 429/5xx 重试改为指数退避 + jitter

**Files:**
- Modify: `AILLMGateway/src/main/java/com/llmgateway/service/UpstreamClient.java`

**Step 1: 找到重试循环，将固定 sleep 改为指数退避**

```java
// 重试间隔：指数退避 + 随机抖动（1s, 2s, 4s...）
long delay = (long) (1000L * (1L << attempt)) + ThreadLocalRandom.current().nextInt(500);
Thread.sleep(delay);
```

**Step 2: 验证编译**

```bash
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AILLMGateway\\pom.xml" compile -q
```

**Step 3: Commit**

- [ ] 完成

---

### Task 3.4: [F4] AgentOrchestratorSupport 视频轮询硬上限

**Objective:** 防止上游永久 queued → 轮询永不结束

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/agent/handler/AgentOrchestratorSupport.java`

**Step 1: 在 pollVideoTaskAndUpdate 中加 10 分钟超时**

```java
private void pollVideoTaskAndUpdate(String taskId) {
    long deadline = System.currentTimeMillis() + 600_000; // 10 分钟硬上限
    while (System.currentTimeMillis() < deadline) {
        // ... 现有轮询逻辑
        if ("completed".equals(status) || "failed".equals(status)) return;
        Thread.sleep(5000);
    }
    // 超时：标记失败
    log.warn("视频轮询超时(10min): taskId={}", taskId);
    // 更新 agent_assets 状态为 failed
}
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 3.5: [F5] GatewayModelServiceImpl 网关不可达冷却

**Objective:** 网关不可达时不再每次 HTTP 重试，冷却 5 分钟

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/ai/impl/GatewayModelServiceImpl.java`

**Step 1: 加冷却时间戳**

```java
private volatile long gatewayUnavailableUntil = 0;

private String getDefaultModel(String type) {
    return defaultCache.computeIfAbsent(type, t -> {
        // 网关冷却期内直接走兜底
        if (System.currentTimeMillis() < gatewayUnavailableUntil) {
            return fallbackFor(t);
        }
        String fetched = fetchDefaultModel(t);
        if (fetched != null) return fetched;
        // 网关不可达：标记冷却
        gatewayUnavailableUntil = System.currentTimeMillis() + 300_000;
        return fallbackFor(t);
    });
}

private String fallbackFor(String type) {
    if ("text".equals(type)) return config.getGateway().getFallbackTextModel();
    return FALLBACK_DEFAULTS.getOrDefault(type, "");
}
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

## Phase 4: 异常场景补全（E1-E4）

### Task 4.1: [E1] RuntimeException → BusinessException（批量）

**Objective:** 45 处 RuntimeException 改为带正确错误码的 BusinessException

**Files:**
- Modify: 所有 `service/ai/impl/` 和 `service/agent/impl/` 下抛 RuntimeException 的文件

**Step 1: 按语义分类替换**

| 语义 | 错误码 | 示例 |
|------|--------|------|
| 参数校验失败 | 40001 | "分镜不存在" / "prompt 不能为空" / "上传文件为空" |
| 资源不存在 | 40401 | "分镜不存在: " + sceneId |
| 上游 AI 失败 | 50201 | "AI 图片生成失败" / "Video API returned" |
| 上游超时 | 50202 | "AI 调用超时" |

批量替换模式：
```java
// 旧
throw new RuntimeException("分镜不存在: " + sceneId);
// 新
throw new BusinessException(40401, "分镜不存在: " + sceneId);

// 旧
throw new RuntimeException("AI 图片生成失败: " + e.getMessage(), e);
// 新
throw new BusinessException(50201, "AI 图片生成失败: " + e.getMessage());
```

需要 import `com.storyboard.exception.BusinessException`。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 4.2: [E2] 静默 catch 加 log.debug

**Objective:** 5 处空 catch 至少加 log.debug

**Files:**
- `AisplitIntentHandler.java:344`
- `AgentOrchestratorSupport.java:975`
- `AgentAnswerServiceImpl.java:89`
- `ImageRefinePromptServiceImpl.java:162`
- `VideoPlanServiceImpl.java:182`

**Step 1: 每处空 catch 加 log.debug**

```java
// 旧
} catch (Exception ignored) {}
// 新
} catch (Exception e) { log.debug("操作失败(忽略): {}", e.getMessage()); }
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 4.3: [E3] ConversationLock InterruptedException 处理

**Objective:** catch InterruptedException 后正确退出

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/agent/ConversationLock.java`

**Step 1: 找到 catch InterruptedException，加 return**

```java
} catch (InterruptedException e) {
    Thread.currentThread().interrupt();
    return false;  // 或 throw new BusinessException(40901, "获取会话锁被中断");
}
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 4.4: [E4] 关键路径 return null → Optional 或 sentinel（选择性）

**Objective:** 最关键的 3-4 处 return null 改为更安全的模式

**Ponytail:** 全改 21 处 YAGNI。只改调用方容易忘 null check 的：`AgentOrchestratorSupport.planField` 返回 null 被 6 处调用。

**Files:**
- Modify: `AgentOrchestratorSupport.java` 的 planField 方法

**Step 1: planField 返回空字符串而非 null**

```java
public String planField(String plan, String field) {
    if (plan == null) return "";
    // ... 现有逻辑
    // 所有 return null 改为 return ""
}
```

调用方用 `isBlank()` 判断（已有），零改动。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

## Phase 5: 日志规范化（L1-L4）

### Task 5.1: [L1] 新增 logback-spring.xml（两个项目）

**Objective:** 日志轮转、文件输出、级别分离

**Files:**
- Create: `AIStoryboardBackend/src/main/resources/logback-spring.xml`
- Create: `AILLMGateway/src/main/resources/logback-spring.xml`

**Step 1: 创建 logback-spring.xml**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_DIR" value="logs"/>
    <property name="APP_NAME" value="ai-storyboard"/>

    <!-- 控制台 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件：按天轮转，30 天保留，单文件 100MB 上限 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${APP_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/${APP_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <springProfile name="local">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.storyboard" level="DEBUG"/>
    </springProfile>

    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
        <logger name="com.storyboard" level="INFO"/>
    </springProfile>
</configuration>
```

网关版本同理，`APP_NAME` 改为 `llm-gateway`，logger 改 `com.llmgateway`。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 5.2: [L2] FileStorageServiceImpl log.info → log.debug

**Objective:** 减少高频上传日志噪音

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/impl/FileStorageServiceImpl.java`

**Step 1: 4 处 log.info 改 log.debug**

```java
// L67, L128, L156, L200
log.debug("Saved base64 image: {}", target);
log.debug("Saved reference {}: {}", t, targetDir.resolve(filename));
log.debug("Saved uploaded image: {}", target);
log.debug("Downloaded and saved: {} -> {}", sourceUrl, target);
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 5.3: [L3] GlobalExceptionHandler 堆栈截断

**Objective:** 上游 API 错误堆栈无意义，只 log message

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/exception/GlobalExceptionHandler.java:121-132`

**Step 1: handleUnknown 改为分级日志**

```java
@ExceptionHandler(Exception.class)
public ResponseEntity<ApiResponse<Void>> handleUnknown(Exception e) {
    String message = extractReadableError(e.getMessage());
    // 上游 AI 错误（RuntimeException 带 cause）只记 message，不打堆栈
    if (e.getCause() != null && message != null && !message.isBlank()) {
        log.error("Unhandled exception: {}", message);
    } else {
        log.error("Unhandled exception", e);
    }
    if (message == null || message.isBlank()) {
        message = "服务器内部错误";
    } else if (message.length() > 200) {
        message = message.substring(0, 200);
    }
    return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
        .body(ApiResponse.error(50000, message));
}
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 5.4: [L4] 结构化日志（跳过）

**Ponytail:** JSON 日志 encoder 需要额外依赖（logstash-logback-encoder），当前 2GB 内存服务器不值得。跳过，当接入 ELK 时再加。

- [ ] 完成（跳过，YAGNI）

---

## Phase 6: 配置外部化（C2 + C3 注释）

### Task 6.1: [C2] uploads 路径配置化

**Objective:** 硬编码 uploads/images → 从配置读取

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/ai/AiConfigProperties.java`
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/impl/FileStorageServiceImpl.java`
- Modify: `AIStoryboardBackend/src/main/resources/application.yml`

**Step 1: 在 AiConfigProperties 加文件存储路径字段**

```java
/** 图片上传目录 */
private String imageUploadDir = "uploads/images";
/** 音频上传目录 */
private String audioUploadDir = "uploads/audios";
```

在 application.yml 的 `ai:` 段加：
```yaml
ai:
  image-upload-dir: uploads/images
  audio-upload-dir: uploads/audios
```

FileStorageServiceImpl 改为注入 AiConfigProperties，从配置读路径。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 6.2: [C3] AiConfigProperties 硬编码默认值加注释

**Objective:** videoModelAliases / geminiImageModels 硬编码 JSON 标注来源

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/service/ai/AiConfigProperties.java`

**Step 1: 加注释说明硬编码原因**

```java
/**
 * 视频模型别名映射（JSON 字符串）。
 * 硬编码原因：仅影响前端下拉选项文本，不涉及密钥/路由，admin 面板可通过 sys_config 覆盖。
 * 权威源：网关 model_params 表；此处仅为离线兜底。
 */
@Getter
private String videoModelAliases = "{\"veo-3.1-fast\":\"veo-3.1-fast-generate-preview\",...}";
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

## Phase 7: 监控告警体系（M1-M3）

### Task 7.1: [M1] Prometheus metrics 暴露

**Objective:** 加 micrometer + 自定义业务指标

**Files:**
- Modify: `AIStoryboardBackend/pom.xml`
- Modify: `AIStoryboardBackend/src/main/resources/application.yml`

**Step 1: pom.xml 加依赖**

```xml
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-registry-prometheus</artifactId>
</dependency>
```

application.yml 加：
```yaml
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 7.2: [M2] LlmGatewayHealthIndicator 接入告警（跳过）

**Ponytail:** 告警阈值需要 Prometheus + AlertManager 部署，当前单机 2GB 不现实。health indicator 已存在，暴露到 /actuator/health 即可。M1 的 prometheus endpoint 覆盖了指标暴露需求。

- [ ] 完成（跳过，基础设施就绪后再配 AlertManager）

---

### Task 7.3: [M3] 网关 call_logs 失败率统计（跳过）

**Ponytail:** 需要定时任务 + 告警通道，当前无告警通道（无 webhook/邮件）。跳过，M1 的 prometheus metrics 覆盖了"可观察性"需求。

- [ ] 完成（跳过，接入 AlertManager 后再做）

---

## Phase 8: 冷数据归档（D1-D3，软编码）

### Task 8.1: [D1] agent_checkpoints 过期清理定时任务

**Objective:** 清理已消费/过期的 checkpoint（软编码过期天数）

**Files:**
- Create: `AIStoryboardBackend/src/main/java/com/storyboard/service/agent/CheckpointCleanupService.java`

**Step 1: 创建清理 Service**

```java
@Service
@RequiredArgsConstructor
@Slf4j
public class CheckpointCleanupService {

    private final AgentCheckpointMapper checkpointMapper;

    /** 清理过期 checkpoint（已消费 + 超过 retentionDays 天） */
    @Scheduled(cron = "${ai.cleanup.checkpoint-cron:0 0 3 * * *}")  // 默认每天凌晨 3 点
    public void cleanup() {
        int retentionDays = 30;  // ponytail: 可改为从 sys_config 读取
        int deleted = checkpointMapper.delete(new LambdaQueryWrapper<AgentCheckpoint>()
                .in(AgentCheckpoint::getStatus, "used", "expired")
                .lt(AgentCheckpoint::getExpirationTime, OffsetDateTime.now().minusDays(retentionDays)));
        if (deleted > 0) {
            log.info("清理过期 checkpoint: {} 条", deleted);
        }
    }
}
```

需要在主类加 `@EnableScheduling`。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 8.2: [D2] 网关 call_logs 归档（跳过）

**Ponytail:** 需要定时 DELETE + 按月分区。当前 call_logs 表量不大（单机低流量），30 天数据估计 < 10MB。跳过，当表超过 100MB 时再做分区。

- [ ] 完成（跳过，YAGNI）

---

### Task 8.3: [D3] uploads 孤立文件清理

**Objective:** 清理无 DB 引用的生成文件

**Files:**
- Create: `AIStoryboardBackend/src/main/java/com/storyboard/service/OrphanFileCleanupService.java`

**Step 1: 创建清理 Service**

```java
@Service
@RequiredArgsConstructor
@Slf4j
public class OrphanFileCleanupService {

    private final SceneMapper sceneMapper;

    /** 清理 uploads/ 下无 DB 引用的文件（软编码保留天数） */
    @Scheduled(cron = "${ai.cleanup.file-cron:0 0 4 * * *}")  // 默认每天凌晨 4 点
    public void cleanup() {
        // ponytail: 简单实现——扫描 uploads/images 和 uploads/videos，
        // 对比 scenes 表中 image_url/video_url 和 agent_assets.url，
        // 无引用且文件龄 > retentionDays 的删除。
        // 实现略——需要先确认哪些表引用了文件路径。
    }
}
```

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

## Phase 9: 接口标准化（A1-A3）

### Task 9.1: [A1] Controller @RequestBody 统一加 @Valid

**Objective:** 所有 Agent 接口入参加校验

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/controller/AgentConversationController.java`
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/controller/ProjectController.java`
- Modify: 各 request record

**Step 1: 批量加 @Valid**

对所有 `@RequestBody` 参数加 `@Valid`，对应 record 加 `@NotBlank`/`@Size` 等。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 9.2: [A2] getConversation 返回 Map → 类型安全 VO

**Objective:** 消除 Map<String, Object> 返回值

**Files:**
- Modify: `AIStoryboardBackend/src/main/java/com/storyboard/controller/AgentConversationController.java`
- Create: `AIStoryboardBackend/src/main/java/com/storyboard/dto/response/AgentConversationDetailVO.java`

**Step 1: 创建 VO record**

```java
public record AgentConversationDetailVO(
    AgentConversationVO conversation,
    List<AgentMessageVO> messages
) {}
```

Controller 返回 `ApiResponse<AgentConversationDetailVO>`。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 9.3: [A3] 网关 ModelParamsRequest 加校验注解

**Objective:** modelName / type 加 @NotBlank

**Files:**
- Modify: `AILLMGateway/src/main/java/com/llmgateway/dto/admin/ModelParamsRequest.java`

**Step 1: 加注解**

```java
public record ModelParamsRequest(
    @jakarta.validation.constraints.NotBlank String modelName,
    @jakarta.validation.constraints.NotBlank String type,
    Boolean isDefault,
    // ... 其余字段
) {}
```

Controller 对应加 `@Valid`。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

## Phase 10: 边界隔离（B1-B3）

### Task 10.1: [B1] AiConfigProperties gateway final 确认（跳过）

**Ponytail:** 上次审计已改为 `final` + 无 setter，当前代码已正确。无需改动。

- [ ] 完成（跳过，已修复）

---

### Task 10.2: [B2] UpstreamClient @DependsOn → lazy init

**Objective:** 消除硬编码 bean 名称依赖

**Files:**
- Modify: `AILLMGateway/src/main/java/com/llmgateway/service/UpstreamClient.java`

**Step 1: 改为 @Lazy 注入 + @PostConstruct 初始化 HttpClient**

```java
@Component
public class UpstreamClient {

    private final GatewayConfig gatewayConfig;
    private HttpClient httpClient;

    public UpstreamClient(GatewayConfig gatewayConfig) {
        this.gatewayConfig = gatewayConfig;
    }

    @PostConstruct
    void init() {
        this.httpClient = HttpClient.newBuilder()
                .connectTimeout(Duration.ofMillis(gatewayConfig.getUpstream().getConnectTimeoutMs()))
                .build();
    }
}
```

去掉 `@DependsOn`，GatewayConfig 在构造时已被 SysConfigServiceImpl @PostConstruct 填充（Spring 保证 @PostConstruct 按依赖顺序执行）。

**Ponytail:** 如果仍有顺序问题，改 `@DependsOn` 为注入 `SysConfigService` 接口（不依赖 impl 名称）。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 10.3: [B3] 前端 SSE 解析抽公共函数

**Objective:** sendMessage / submitHumanInput 各自一套 SSE 解析 → 公共函数

**Files:**
- Modify: `AIStoryboardClient/src/stores/agentStore.ts`

**Step 1: 抽取 parseSSEStream 公共函数**

```typescript
async function* parseSSEStream(reader: ReadableStreamDefaultReader<Uint8Array>): AsyncGenerator<SseEvent> {
    const decoder = new TextDecoder();
    let buffer = '';
    while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        buffer += decoder.decode(value, { stream: true });
        // ... 现有 SSE 解析逻辑
    }
}
```

sendMessage 和 submitHumanInput 都改为 `for await (const event of parseSSEStream(reader))`。

**Step 2: 验证前端构建**

```bash
cd AIStoryboardClient && npx tsc -p tsconfig.app.json --noEmit && npm run build
```

**Step 3: Commit**

- [ ] 完成

---

## Phase 11: 技术债务清理（T1-T4）

### Task 11.1: [T1] AgentOrchestratorSupport 拆分（跳过）

**Ponytail:** 1100 行拆分需要大规模重构 + 全量回归测试，投入产出比低。当前功能正确，拆分收益是可维护性而非正确性。跳过，下次大功能迭代时顺带拆。

- [ ] 完成（跳过，下次迭代顺带）

---

### Task 11.2: [T2] ChatClient 懒加载 DCL → 工具方法

**Objective:** 8 个 Service 各自维护 volatile + synchronized DCL → 统一工具方法

**Files:**
- Create: `AIStoryboardBackend/src/main/java/com/storyboard/service/ai/ChatClientFactory.java`

**Step 1: 创建工厂方法**

```java
@Component
public class ChatClientFactory {

    private final ChatClient.Builder builder;

    public ChatClientFactory(ChatClient.Builder builder) {
        this.builder = builder;
    }

    /** 创建懒加载 ChatClient 的 Supplier（线程安全，DCL 内置） */
    public Supplier<ChatClient> lazy(String model, Duration timeout) {
        return new Supplier<>() {
            private volatile ChatClient client;
            @Override
            public ChatClient get() {
                if (client == null) {
                    synchronized (this) {
                        if (client == null) {
                            client = builder.defaultOptions(
                                OpenAiChatOptions.builder().model(model).timeout(timeout)
                            ).build();
                        }
                    }
                }
                return client;
            }
        };
    }
}
```

各 Service 注入 ChatClientFactory，用 `chatClient = chatClientFactory.lazy(model, timeout)` 替代手写 DCL。

**Step 2: 验证编译**

**Step 3: Commit**

- [ ] 完成

---

### Task 11.3: [T3] MultipartBuilder 可移除性确认（跳过）

**Ponytail:** Spring AI 2.0 仍无 images/edits multipart 适配（CLAUDE.md 已记录），MultipartBuilder 仍需保留。

- [ ] 完成（跳过，Spring AI 不支持）

---

### Task 11.4: [T4] .env.example 删除后文档更新

**Objective:** 更新 README 说明配置方式已改为 Spring Profile

**Files:**
- Modify: `CLAUDE.md`（已更新）
- Create: `AIStoryboardBackend/src/main/resources/application-local.yml.example`
- Create: `AILLMGateway/src/main/resources/application-local.yml.example`

**Step 1: 创建 example profile 文件（脱敏模板）**

```yaml
# application-local.yml.example — 复制为 application-local.yml 并填入真实值
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/newworkflow
    username: postgres
    password: <your-password>
  ai:
    openai:
      base-url: http://localhost:8083/v1
      api-key: <your-gateway-api-key>

jwt:
  access-secret: <random-32char-hex>
  refresh-secret: <random-32char-hex>

ai:
  gateway:
    base-url: http://localhost:8083
    api-key: <your-gateway-api-key>
```

**Step 2: Commit**

- [ ] 完成

---

## Phase 12: 体验优化（U1-U3）

### Task 12.1: [U1] 前端 SSE 断连自动重连（跳过）

**Ponytail:** SSE 断连场景罕见（用户主动关闭/网络中断），EventSource 自带重连但项目用的是 fetch + ReadableStream（因为需要 POST body）。重连需重建整个编排上下文，复杂度高。跳过，用户手动刷新即可。

- [ ] 完成（跳过，YAGNI）

---

### Task 12.2: [U2] 前端刷新时从后端恢复 generating 状态

**Objective:** 页面刷新后 generating 状态的场景显示"重试"按钮

**Ponytail:** 后端 Task 2.1 已加 `resetStaleGenerating`（5 分钟后自动 failed），前端刷新时后端会返回正确状态。前端只需信任后端返回的 status，无需额外处理。

- [ ] 完成（后端 P1 已覆盖）

---

### Task 12.3: [U3] 前端空 catch 加 toast 提示

**Objective:** 用户操作失败时至少显示 toast

**Files:**
- Modify: `AIStoryboardClient/src/stores/agentStore.ts`
- Modify: `AIStoryboardClient/src/components/agent/AgentChatPanel.tsx`

**Step 1: 关键操作的 catch 加 toast**

以 agentStore.sendMessage 为例：
```typescript
} catch (err) {
    // 旧: 空 catch
    // 新:
    useProjectStore.getState().showToast?.('发送失败，请重试', 'error');
}
```

只改用户主动触发的操作（发送消息、上传、提交表单），被动轮询的 catch 保持静默。

**Step 2: 验证前端构建**

**Step 3: Commit**

- [ ] 完成

---

## 总结

| Phase | 内容 | Task 数 | 跳过 | 实际改动 |
|-------|------|---------|------|----------|
| 1 | 安全 S1+S4 | 2 | 0 | 2 |
| 2 | 性能 P1-P8 | 8 | 0 | 8 |
| 3 | 故障处置 F1-F5 | 5 | 0 | 5 |
| 4 | 异常补全 E1-E4 | 4 | 0 | 4 |
| 5 | 日志 L1-L4 | 4 | 1(L4) | 3 |
| 6 | 配置 C2+C3 | 2 | 0 | 2 |
| 7 | 监控 M1-M3 | 3 | 2(M2,M3) | 1 |
| 8 | 冷数据 D1-D3 | 3 | 1(D2) | 2 |
| 9 | 接口 A1-A3 | 3 | 0 | 3 |
| 10 | 边界 B1-B3 | 3 | 1(B1) | 2 |
| 11 | 技术债 T1-T4 | 4 | 2(T1,T3) | 2 |
| 12 | 体验 U1-U3 | 3 | 1(U1) | 2 |
| **合计** | | **44** | **8** | **36** |

**验证命令（每个 Task 完成后执行）：**
```bash
# 后端
export JAVA_HOME="C:\\Program Files\\Java\\jdk-21"
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q

# 网关
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AILLMGateway\\pom.xml" compile -q

# 前端
cd AIStoryboardClient && npx tsc -p tsconfig.app.json --noEmit && npm run build
```
