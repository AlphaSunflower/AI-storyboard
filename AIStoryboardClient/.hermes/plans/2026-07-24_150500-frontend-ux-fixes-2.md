# Frontend UX 第二波修复计划

**Goal:** 历史栏移至左栏下方、分镜重命名、参考图导入、模型选择器

---

### 任务 A：历史栏移至左栏 + 剧本面板紧靠左边

**修改:**
- AIStoryboardClient/src/pages/EditorPage.tsx — 删除顶部 ProjectHistoryPanel，改用侧栏嵌入
- AIStoryboardClient/src/components/editor/ScriptInputPanel.tsx — 底部嵌入 ProjectHistoryPanel

在 ScriptInputPanel 底部（折叠按钮下方）渲染 ProjectHistoryPanel：
```tsx
{!collapsed && <ProjectHistoryPanel />}
```

ProjectHistoryPanel 改为竖直列表（非横向胶囊），compact 样式。

---

### 任务 B：分镜重命名

**修改:** AIStoryboardClient/src/components/scene/SceneCard.tsx

在卡片标题"分镜 X"旁添加 ✏️ → 弹出 input inline → enter 提交 → 调用 sceneApi.update(id, {scriptContent: newName})

---

### 任务 C：参考图上传

**修改:** AIStoryboardClient/src/components/editor/ScriptInputPanel.tsx

在剧本输入区上方添加图片上传按钮：
```tsx
<input type="file" accept="image/*" onChange={...} />
```
预览缩略图，上传后存为 data URL 或调用后端上传接口。

---

### 任务 D：图片/视频模型选择器

**修改:**
- AIStoryboardClient/src/components/editor/ScriptInputPanel.tsx — 添加两个下拉
- AIStoryboardClient/src/stores/projectStore.ts — 添加 imageModel + videoModel 全局状态
- AIStoryboardClient/src/components/scene/SceneCard.tsx — 生成时使用 store 中的模型

模型列表：
- 生图: gpt-image-2, gemini-3-pro-image-preview
- 生视频: veo-3.1-fast, veo-3.1

SceneCard 的 generateImage/generateVideo 传 store 中的 selectedModel。
