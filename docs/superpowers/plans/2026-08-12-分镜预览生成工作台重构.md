# 分镜预览生成工作台重构 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。**每完成一个任务必须执行其验证步骤**，验证通过再进入下一个任务。

**目标：** 分镜列表瘦身（只留提示词编辑+信息查看），预览面板重构为生成工作台（图片 tab / 视频 tab 各自独立参数与参考素材，范围全部来自网关），参考图/参考视频/参考音频全链路真上传真生成。

**架构：** 网关扩展视频契约（reference* 数组）+ 新增文件上传代理（mm_file://）；主后端新增场景参考素材端点（改造 scene_reference_images 表）、scenes 表加参数覆盖/多图结果列、generate-image 多图返回、generate-video 参考素材透传；前端 SceneCard 瘦身、PreviewPanel 重构、store 改分镜覆盖语义。

**技术栈：** Spring Boot 4 + MyBatis-Plus + PostgreSQL（主后端）、Spring Boot 4 网关（AILLMGateway）、React 19 + TS + Zustand（前端）。

**前置事实：**
- MiniMax-H3 `POST /v2/video_generation` content 数组 role：`reference_image`(≤9 张/30 MB) / `reference_video`(≤3 个/50 MB/单段 2-15 s) / `reference_audio`(≤3 个/15 MB)，请求体 ≤ 64 MB；**与 first_frame 互斥**。
- 网关 model_params 约束列已齐、`buildParams` 已输出（refImages/refVideos/audioCount/maxImageSizeMB/maxVideoSizeMB/maxAudioSizeMB/maxRequestBodyMB/maxPromptChars 等），image/video 模型共用同批列，**网关组装零改动**。
- 主后端 `MultipartBuilder`（service/ai 包）已存在，可构建 multipart 字节流。
- 主后端 migration 为留档 + psql 手动应用（无 Flyway）。

---

## 文件结构

**AILLMGateway（网关）**
- 修改：`src/main/java/com/llmgateway/service/impl/VideoGatewayServiceImpl.java`（createMinimax 契约扩展 + 互斥校验）
- 修改：`src/main/java/com/llmgateway/service/UpstreamClient.java`（postMultipart 加超时参数重载）
- 新建：`src/main/java/com/llmgateway/service/FileUploadService.java`（接口）
- 新建：`src/main/java/com/llmgateway/service/impl/FileUploadServiceImpl.java`（转发 MiniMax 上传拿 file_id）
- 修改：`src/main/java/com/llmgateway/controller/OpenAiCompatController.java`（+POST /v1/files/upload）

**AIStoryboardBackend（主后端）**
- 新建：`src/main/resources/db/migration/V6__scene_params_and_refs.sql`（留档，psql 手动应用）
- 修改：`src/main/java/com/storyboard/entity/Scene.java`（+7 列）
- 修改：`src/main/java/com/storyboard/entity/SceneReferenceImage.java`（+type/fileName/fileSize）
- 修改：`src/main/java/com/storyboard/dto/request/SceneRequest.java`、`dto/response/SceneResponse.java`
- 修改：`src/main/java/com/storyboard/dto/request/GenerateVideoRequest.java`（+referenceVideos/referenceAudios）
- 新建：`src/main/java/com/storyboard/dto/response/SceneReferenceResponse.java`
- 修改：`src/main/java/com/storyboard/service/SceneService.java`、`service/impl/SceneServiceImpl.java`（参考素材 CRUD + 新字段同步）
- 修改：`src/main/java/com/storyboard/service/FileStorageService.java`、`service/impl/FileStorageServiceImpl.java`（+saveUploadedReference/resolveAudio）
- 修改：`src/main/java/com/storyboard/controller/FileController.java`（+/api/files/audios/{filename}）
- 修改：`src/main/java/com/storyboard/controller/SceneController.java`（+参考素材端点）
- 修改：`src/main/java/com/storyboard/controller/AIController.java`（imageUrls 返回 + 视频新参数）
- 修改：`src/main/java/com/storyboard/service/ai/impl/ImageGenerationServiceImpl.java`（多图返回与落库）
- 修改：`src/main/java/com/storyboard/service/ai/impl/VideoGenerationServiceImpl.java`（参考素材组装/转换/互斥）

**AIStoryboardClient（前端）**
- 修改：`src/api/projects.ts`（SceneResponse 新字段 + 素材类型）、`src/api/scenes.ts`（素材端点）、`src/api/ai.ts`（imageUrls/referenceVideos/referenceAudios）
- 修改：`src/config.ts`（静态参考素材上限兜底）
- 修改：`src/stores/projectStore.ts`（移除 getSceneRefs/setSceneRefs；+素材读写/分镜覆盖参数）
- 修改：`src/components/scene/SceneCard.tsx`（瘦身）
- 修改：`src/components/editor/PreviewPanel.tsx`（生成工作台重构）
- 新建：`src/components/editor/ReferenceUploader.tsx`（三类素材上传组件）
- 删除：`src/components/ai/ImageRefineModal.tsx`、`src/components/ai/VideoRefineModal.tsx`（仅 SceneCard 引用）

---

## 阶段 A：网关（AILLMGateway）

### 任务 A1：视频契约扩展（reference* 数组）

**文件：**
- 修改：`AILLMGateway/src/main/java/com/llmgateway/service/impl/VideoGatewayServiceImpl.java`

- [ ] **步骤 1：实现 createMinimax 多模态参考组装**

在 `createMinimax` 的 `textPart` 之后、`imageUrl`（首帧）处理之前插入参考素材解析，并加互斥校验：

```java
// 多模态参考素材（r2va）：referenceImages / referenceVideos / referenceAudios（data URI 或 mm_file://）
// 与图生视频首帧（imageUrl）互斥——MiniMax 契约明确二者不可混用
List<String> refImages = new ArrayList<>();
for (JsonNode n : body.path("referenceImages")) refImages.add(n.asText(""));
List<String> refVideos = new ArrayList<>();
for (JsonNode n : body.path("referenceVideos")) refVideos.add(n.asText(""));
List<String> refAudios = new ArrayList<>();
for (JsonNode n : body.path("referenceAudios")) refAudios.add(n.asText(""));
boolean hasRefs = !refImages.isEmpty() || !refVideos.isEmpty() || !refAudios.isEmpty();
String imageUrl = body.path("imageUrl").asText("");
if (hasRefs && !imageUrl.isBlank()) {
    throw new BusinessException(40001, "多模态参考素材与首帧图生视频互斥，不能同时提交");
}
for (String ref : refImages) {
    ObjectNode part = content.addObject();
    part.put("type", "image_url");
    part.put("image_url", objectMapper.createObjectNode().put("url", ref));
    part.put("role", "reference_image");
}
for (String ref : refVideos) {
    ObjectNode part = content.addObject();
    part.put("type", "video_url");
    part.put("video_url", objectMapper.createObjectNode().put("url", ref));
    part.put("role", "reference_video");
}
for (String ref : refAudios) {
    ObjectNode part = content.addObject();
    part.put("type", "audio_url");
    part.put("audio_url", objectMapper.createObjectNode().put("url", ref));
    part.put("role", "reference_audio");
}
```

原 `String imageUrl = body.path("imageUrl").asText("");` 行删除（已上移）。ratio 逻辑不变（hasRefs 时 r2va 允许 adaptive 或显式值，现有 isImageToVideo 判定改为 `!imageUrl.isBlank()`）。

- [ ] **步骤 2：编译验证**

```bash
export JAVA_HOME="C:\\Program Files\\Java\\jdk-21"
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AILLMGateway\\pom.xml" compile -q
```
预期：BUILD SUCCESS（exit 0）。

- [ ] **步骤 3：Commit**

```bash
cd /e/Desktop/AI-storyboard && git add AILLMGateway/src/main/java/com/llmgateway/service/impl/VideoGatewayServiceImpl.java && git commit -m "feat(gateway): 视频契约扩展 referenceImages/referenceVideos/referenceAudios 多模态参考组装 + 与首帧互斥校验"
```

### 任务 A2：文件上传代理（mm_file://）

**文件：**
- 修改：`AILLMGateway/src/main/java/com/llmgateway/service/UpstreamClient.java`
- 新建：`AILLMGateway/src/main/java/com/llmgateway/service/FileUploadService.java`
- 新建：`AILLMGateway/src/main/java/com/llmgateway/service/impl/FileUploadServiceImpl.java`
- 修改：`AILLMGateway/src/main/java/com/llmgateway/controller/OpenAiCompatController.java`

- [ ] **步骤 1：UpstreamClient 加超时参数重载**

```java
/** POST multipart（上传大文件用长超时，如 MiniMax 文件上传 50MB） */
public HttpResponse<String> postMultipart(String baseUrl, String path, String apiKey,
                                          String contentType, byte[] bodyBytes, long timeoutMs) {
    HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create(stripTrailingSlash(baseUrl) + path))
            .header("Content-Type", contentType)
            .header("Authorization", "Bearer " + apiKey)
            .timeout(Duration.ofMillis(timeoutMs))
            .POST(HttpRequest.BodyPublishers.ofByteArray(bodyBytes))
            .build();
    return sendWithRetry(request);
}
```

- [ ] **步骤 2：新建 FileUploadService 接口**

```java
package com.llmgateway.service;

/** 上游文件上传代理：返回 mm_file://{file_id}（供视频多模态参考引用） */
public interface FileUploadService {
    /**
     * 上传文件到上游（MiniMax POST /v1/files/upload）并返回 mm_file://{file_id}
     * @param contentType 原始 Content-Type（含 multipart boundary）
     * @param bodyBytes   multipart 请求体字节流
     */
    String upload(String contentType, byte[] bodyBytes);
}
```

- [ ] **步骤 3：实现 FileUploadServiceImpl**

关键点：按 model=MiniMax-H3 解析路由（复用 VideoGatewayServiceImpl 的渠道解析语义——为避免重复代码，本实现直接遍历 enabled 渠道找 type=minimax 的第一个，找不到抛 50301）；转发 `POST {base}/v1/files/upload`（长超时 300s）；解析响应的 `file_id`（`file.file_id` 或顶层 `file_id` 兜底）；返回 `mm_file://{file_id}`。**purpose 参数：先不带；若上游 400 提示 purpose 必填，在 URL 加 `?purpose=video_generation` 重试一次**（voice_clone 示例带 purpose，视频场景文档未明示，留兼容分支）。

```java
package com.llmgateway.service.impl;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.llmgateway.entity.Channel;
import com.llmgateway.exception.BusinessException;
import com.llmgateway.mapper.ChannelMapper;
import com.llmgateway.service.FileUploadService;
import com.llmgateway.service.KeyService;
import com.llmgateway.service.UpstreamClient;
import lombok.RequiredArgsConstructor;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
@RequiredArgsConstructor
public class FileUploadServiceImpl implements FileUploadService {

    private static final Logger log = LoggerFactory.getLogger(FileUploadServiceImpl.class);
    private static final long UPLOAD_TIMEOUT_MS = 300_000L;

    private final ChannelMapper channelMapper;
    private final KeyService keyService;
    private final UpstreamClient upstreamClient;
    private final ObjectMapper objectMapper = new ObjectMapper();

    @Override
    public String upload(String contentType, byte[] bodyBytes) {
        // 文件上传固定走 minimax 渠道（MiniMax mm_file:// 生态；Laozhang 无对应机制）
        List<Channel> minimaxChannels = channelMapper.selectList(new LambdaQueryWrapper<Channel>()
                .eq(Channel::getType, "minimax")
                .eq(Channel::getEnabled, true)
                .orderByAsc(Channel::getPriority));
        if (minimaxChannels.isEmpty()) {
            throw new BusinessException(50301, "no available minimax channel for file upload");
        }
        Channel channel = minimaxChannels.getFirst();
        String apiKey = keyService.decrypt(channel.getApiKey());
        String base = channel.getBaseUrl().endsWith("/")
                ? channel.getBaseUrl().substring(0, channel.getBaseUrl().length() - 1)
                : channel.getBaseUrl();

        Exception lastErr = null;
        for (String suffix : new String[] { "/v1/files/upload", "/v1/files/upload?purpose=video_generation" }) {
            try {
                var resp = upstreamClient.postMultipart(base, suffix, apiKey, contentType, bodyBytes, UPLOAD_TIMEOUT_MS);
                if (resp.statusCode() != 200) {
                    String err = upstreamClient.extractError(resp.body());
                    log.warn("MiniMax 文件上传返回 {}: {}", resp.statusCode(), err);
                    // 400 且 body 提及 purpose/参数错误 → 换带 purpose 的 URL 重试；否则直接抛
                    if (!(resp.statusCode() == 400 && (resp.body().contains("purpose") || resp.body().contains("param")))) {
                        throw new BusinessException(50201, "upstream file upload failed: " + err);
                    }
                    continue;
                }
                JsonNode root = objectMapper.readTree(resp.body());
                String fileId = root.path("file").path("file_id").asText(
                        root.path("file_id").asText(""));
                if (fileId.isBlank()) {
                    throw new BusinessException(50201, "upstream file upload missing file_id: " + resp.body());
                }
                log.info("MiniMax 文件上传成功: file_id={}", fileId);
                return "mm_file://" + fileId;
            } catch (BusinessException be) {
                throw be;
            } catch (Exception e) {
                lastErr = e;
            }
        }
        throw new BusinessException(50201, "upstream file upload failed: "
                + (lastErr == null ? "unknown" : lastErr.getMessage()));
    }
}
```

- [ ] **步骤 4：OpenAiCompatController 加端点**

```java
/** 文件上传代理：原始 multipart 字节流 + Content-Type（含 boundary）→ MiniMax，返回 mm_file://{file_id} */
@PostMapping(value = "/files/upload", produces = MediaType.APPLICATION_JSON_VALUE)
public ResponseEntity<String> fileUpload(@RequestBody byte[] body,
                                         @RequestHeader("Content-Type") String contentType) {
    String fileId = fileUploadService.upload(contentType, body);
    return ResponseEntity.ok("{\"file_id\":\"" + fileId + "\"}");
}
```

构造器注入 `FileUploadService`。

- [ ] **步骤 5：编译验证**

```bash
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AILLMGateway\\pom.xml" compile -q
```
预期：BUILD SUCCESS。

- [ ] **步骤 6：契约冒烟（桩验证，无需上游密钥）**

用临时 main 或 JUnit（mvn ad-hoc 配方见技能 reference）验证：上传代理在无 minimax 渠道时抛 50301（可跳过，编译即可）；`createMinimax` 的互斥校验：构造含 referenceImages + imageUrl 的 body 应抛 40001。若时间有限，以编译 + 人工代码走查为最低门槛。

- [ ] **步骤 7：Commit**

```bash
cd /e/Desktop/AI-storyboard && git add AILLMGateway && git commit -m "feat(gateway): /v1/files/upload 文件上传代理——转发 MiniMax 拿 file_id 返回 mm_file://（长超时+失败回退 purpose 参数）"
```

**阶段 A 完成标志：** 网关编译通过，视频契约与上传代理代码就位。

---

## 阶段 B：主后端（AIStoryboardBackend）

### 任务 B1：数据模型（实体 + DTO + migration 留档）

**文件：**
- 新建：`AIStoryboardBackend/src/main/resources/db/migration/V6__scene_params_and_refs.sql`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/entity/Scene.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/entity/SceneReferenceImage.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/dto/request/SceneRequest.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/dto/response/SceneResponse.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/service/impl/SceneServiceImpl.java`（updateScene/toResponse 同步）

- [ ] **步骤 1：写 migration SQL（留档）**

```sql
-- V6__scene_params_and_refs.sql
-- 分镜生成参数覆盖（null = 跟随全局默认）+ 多图结果
ALTER TABLE scenes
    ADD COLUMN image_urls TEXT,
    ADD COLUMN image_model VARCHAR(128),
    ADD COLUMN image_size VARCHAR(64),
    ADD COLUMN image_quality VARCHAR(32),
    ADD COLUMN image_n INT,
    ADD COLUMN video_model VARCHAR(128),
    ADD COLUMN video_aspect_ratio VARCHAR(32);

-- 参考素材表通用化（原 scene_reference_images 基本未用）
ALTER TABLE scene_reference_images
    ADD COLUMN type VARCHAR(16) NOT NULL DEFAULT 'image',
    ADD COLUMN file_name VARCHAR(255),
    ADD COLUMN file_size BIGINT;
```

- [ ] **步骤 2：Scene 实体加 7 字段**

```java
private String imageUrls;
private String imageModel;
private String imageSize;
private String imageQuality;
private Integer imageN;
private String videoModel;
private String videoAspectRatio;
```

- [ ] **步骤 3：SceneReferenceImage 实体加 3 字段**

```java
private String type;
private String fileName;
private Long fileSize;
```

- [ ] **步骤 4：SceneRequest 加字段**

```java
// 追加到 record：
String imageModel,
String imageSize,
String imageQuality,
Integer imageN,
String videoModel,
String videoAspectRatio
```

- [ ] **步骤 5：SceneResponse 加字段（含 imageUrls + 覆盖参数）**

```java
// 在 videoStatus 之后追加：
String imageUrls,
String imageModel,
String imageSize,
String imageQuality,
Integer imageN,
String videoModel,
String videoAspectRatio
```

- [ ] **步骤 6：SceneServiceImpl 同步**

`updateScene` 追加（null 不修改语义，与现有字段一致）：

```java
if (request.imageModel() != null) scene.setImageModel(request.imageModel());
if (request.imageSize() != null) scene.setImageSize(request.imageSize());
if (request.imageQuality() != null) scene.setImageQuality(request.imageQuality());
if (request.imageN() != null) scene.setImageN(request.imageN());
if (request.videoModel() != null) scene.setVideoModel(request.videoModel());
if (request.videoAspectRatio() != null) scene.setVideoAspectRatio(request.videoAspectRatio());
```

`toResponse` 按新 record 顺序传参。

- [ ] **步骤 7：编译验证**

```bash
export JAVA_HOME="C:\\Program Files\\Java\\jdk-21"
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q
```
预期：BUILD SUCCESS。

- [ ] **步骤 8：Commit**

```bash
cd /e/Desktop/AI-storyboard && git add AIStoryboardBackend/src/main/java/com/storyboard/entity AIStoryboardBackend/src/main/java/com/storyboard/dto AIStoryboardBackend/src/main/java/com/storyboard/service/impl/SceneServiceImpl.java AIStoryboardBackend/src/main/resources/db/migration/V6__scene_params_and_refs.sql && git commit -m "feat: scenes 表参数覆盖/多图结果列 + 参考素材表通用化（实体/DTO/Service 同步）"
```

### 任务 B2：参考素材存储与端点

**文件：**
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/service/FileStorageService.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/service/impl/FileStorageServiceImpl.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/controller/FileController.java`
- 新建：`AIStoryboardBackend/src/main/java/com/storyboard/dto/response/SceneReferenceResponse.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/service/SceneService.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/service/impl/SceneServiceImpl.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/controller/SceneController.java`

- [ ] **步骤 1：FileStorageService 加接口方法**

```java
/** 保存分镜参考素材（image→images 目录，video→videos，audio→audios），返回 /api/files/... 相对 URL */
String saveUploadedReference(String type, org.springframework.web.multipart.MultipartFile file);

/** 解析音频本地文件路径 */
Path resolveAudio(String filename);
```

- [ ] **步骤 2：FileStorageServiceImpl 实现**

```java
private static final Path AUDIOS_DIR = Paths.get("uploads/audios");
// 构造器补：Files.createDirectories(AUDIOS_DIR);
private static final Set<String> ALLOWED_AUDIO_EXTENSIONS = Set.of("wav", "mp3", "m4a");

@Override
public String saveUploadedReference(String type, MultipartFile file) {
    try {
        if (file == null || file.isEmpty()) throw new RuntimeException("上传文件为空");
        String contentType = file.getContentType();
        Path targetDir; String urlPrefix;
        switch (type == null ? "image" : type) {
            case "video" -> {
                if (contentType == null || !(contentType.startsWith("video/") || contentType.contains("quicktime")))
                    throw new RuntimeException("仅支持上传视频文件");
                targetDir = VIDEOS_DIR; urlPrefix = "/api/files/videos/";
            }
            case "audio" -> {
                if (contentType == null || !contentType.startsWith("audio/"))
                    throw new RuntimeException("仅支持上传音频文件");
                targetDir = AUDIOS_DIR; urlPrefix = "/api/files/audios/";
            }
            default -> {
                if (contentType == null || !contentType.startsWith("image/"))
                    throw new RuntimeException("仅支持上传图片文件");
                targetDir = IMAGES_DIR; urlPrefix = "/api/files/images/";
            }
        }
        String original = file.getOriginalFilename();
        String extension = switch (type) {
            case "video" -> "mp4";
            case "audio" -> "mp3";
            default -> "png";
        };
        if (original != null && original.contains(".")) {
            String raw = original.substring(original.lastIndexOf('.') + 1).toLowerCase();
            boolean allowed = "image".equals(type) ? ALLOWED_IMAGE_EXTENSIONS.contains(raw)
                    : "audio".equals(type) ? ALLOWED_AUDIO_EXTENSIONS.contains(raw)
                    : Set.of("mp4", "mov").contains(raw);
            if (allowed) extension = raw;
        }
        String filename = UUID.randomUUID().toString() + "." + extension;
        Files.write(targetDir.resolve(filename), file.getBytes());
        return urlPrefix + filename;
    } catch (IOException e) {
        throw new RuntimeException("保存参考素材失败: " + e.getMessage(), e);
    }
}

@Override
public Path resolveAudio(String filename) { return AUDIOS_DIR.resolve(filename); }
```

- [ ] **步骤 3：FileController 加音频映射**

```java
@GetMapping("/audios/{filename}")
public ResponseEntity<Resource> getAudio(@PathVariable String filename) throws IOException {
    Path filePath = fileStorageService.resolveAudio(filename);
    if (!Files.exists(filePath)) return ResponseEntity.notFound().build();
    var contentType = MediaType.parseMediaType(FileStorageService.contentType(filename));
    FileInputStream fis = new FileInputStream(filePath.toFile());
    return ResponseEntity.ok().contentType(contentType).contentLength(Files.size(filePath))
            .body(new InputStreamResource(fis));
}
```

- [ ] **步骤 4：SceneReferenceResponse（record）**

```java
package com.storyboard.dto.response;

public record SceneReferenceResponse(
    String id,
    String type,
    String url,
    String fileName,
    Long fileSize
) {}
```

- [ ] **步骤 5：SceneService 接口加方法**

```java
/** 分镜参考素材列表（按 sort_order） */
List<SceneReferenceResponse> listReferences(String sceneId);

/** 上传参考素材（type: image/video/audio；校验类型/数量上限/单文件大小上限），返回记录 */
SceneReferenceResponse uploadReference(String sceneId, String type,
        org.springframework.web.multipart.MultipartFile file);

/** 删除参考素材（含本地文件） */
void deleteReference(String referenceId);
```

- [ ] **步骤 6：SceneServiceImpl 实现**

校验上限（对齐 MiniMax 输入约束；网关 params 精确值由前端展示，后端用硬上限兜底）：

```java
private static final Map<String, int[]> REF_LIMITS = Map.of(
    "image", new int[] { 10, 30 * 1024 * 1024 },   // 数量, 单文件 MB
    "video", new int[] { 3, 50 * 1024 * 1024 },
    "audio", new int[] { 3, 15 * 1024 * 1024 });

@Override
public List<SceneReferenceResponse> listReferences(String sceneId) {
    Scene scene = sceneMapper.selectById(sceneId);
    if (scene == null) throw new BusinessException(40401, "分镜不存在");
    return refImageMapper.findBySceneId(sceneId).stream()
        .map(r -> new SceneReferenceResponse(r.getId(), r.getType(), r.getImageUrl(),
                r.getFileName(), r.getFileSize()))
        .toList();
}

@Override
@Transactional
public SceneReferenceResponse uploadReference(String sceneId, String type, MultipartFile file) {
    Scene scene = sceneMapper.selectById(sceneId);
    if (scene == null) throw new BusinessException(40401, "分镜不存在");
    String t = type == null ? "image" : type;
    if (!REF_LIMITS.containsKey(t)) throw new BusinessException(40001, "不支持的素材类型: " + t);
    int[] limits = REF_LIMITS.get(t);
    long existing = refImageMapper.selectCount(new LambdaQueryWrapper<SceneReferenceImage>()
            .eq(SceneReferenceImage::getSceneId, sceneId)
            .eq(SceneReferenceImage::getType, t));
    if (existing >= limits[0]) throw new BusinessException(40001, "参考" + ("video".equals(t) ? "视频" : "audio".equals(t) ? "音频" : "图") + "数量已达上限 " + limits[0]);
    if (file.getSize() > limits[1]) throw new BusinessException(40001, "文件超过大小限制 " + (limits[1] / 1024 / 1024) + "MB");
    String url = fileStorageService.saveUploadedReference(t, file);
    SceneReferenceImage ref = new SceneReferenceImage();
    ref.setSceneId(sceneId);
    ref.setType(t);
    ref.setImageUrl(url);
    ref.setFileName(file.getOriginalFilename());
    ref.setFileSize(file.getSize());
    ref.setSortOrder((int) existing);
    refImageMapper.insert(ref);
    return new SceneReferenceResponse(ref.getId(), t, url, ref.getFileName(), ref.getFileSize());
}

@Override
public void deleteReference(String referenceId) {
    SceneReferenceImage ref = refImageMapper.selectById(referenceId);
    if (ref == null) throw new BusinessException(40401, "素材不存在");
    // 删除本地文件（失败仅警告，不阻断）
    try {
        String filename = ref.getImageUrl().substring(ref.getImageUrl().lastIndexOf('/') + 1);
        Path p = switch (ref.getType()) {
            case "video" -> fileStorageService.resolveVideo(filename);
            case "audio" -> fileStorageService.resolveAudio(filename);
            default -> fileStorageService.resolveImage(filename);
        };
        Files.deleteIfExists(p);
    } catch (IOException e) {
        log.warn("删除参考素材本地文件失败: {}", e.getMessage());
    }
    refImageMapper.deleteById(referenceId);
}
```

需要注入 `fileStorageService` 到 SceneServiceImpl（@RequiredArgsConstructor 加 final 字段）+ LambdaQueryWrapper import。

- [ ] **步骤 7：SceneController 加端点**

```java
@GetMapping("/scenes/{id}/references")
public ApiResponse<List<SceneReferenceResponse>> listReferences(@PathVariable String id) {
    return ApiResponse.ok(sceneService.listReferences(id));
}

@PostMapping("/scenes/{id}/references")
public ApiResponse<SceneReferenceResponse> uploadReference(@PathVariable String id,
        @RequestParam String type, @RequestParam MultipartFile file) {
    return ApiResponse.ok(sceneService.uploadReference(id, type, file));
}

@DeleteMapping("/scenes/references/{referenceId}")
public ApiResponse<Void> deleteReference(@PathVariable String referenceId) {
    sceneService.deleteReference(referenceId);
    return ApiResponse.ok("删除成功", null);
}
```

- [ ] **步骤 8：编译验证**

```bash
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q
```
预期：BUILD SUCCESS。

- [ ] **步骤 9：Commit**

```bash
cd /e/Desktop/AI-storyboard && git add AIStoryboardBackend/src/main/java/com/storyboard/service AIStoryboardBackend/src/main/java/com/storyboard/controller AIStoryboardBackend/src/main/java/com/storyboard/dto/response/SceneReferenceResponse.java && git commit -m "feat: 分镜参考素材上传/列表/删除端点——三类素材类型校验+数量大小上限+本地文件存储"
```

### 任务 B3：generate-image 多图返回

**文件：**
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/service/ai/impl/ImageGenerationServiceImpl.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/controller/AIController.java`

- [ ] **步骤 1：callOpenAIImage 改多图返回**

签名改为 `List<String> callOpenAIImage(...)`，Spring AI `ImageResponse.getResults()` 遍历：

```java
private List<String> callOpenAIImage(String model, String prompt, String size, String quality,
                                     String aspectRatio, Integer n) {
    OpenAiImageOptions.Builder builder = OpenAiImageOptions.builder()
            .model(model)
            .size(normalizeImageSize(size, config.getDefaultImageSize()))
            .n((n != null && n > 0) ? n : 1)
            .timeout(Duration.ofSeconds(180))
            .maxRetries(1);
    if (quality != null && !quality.isEmpty()) builder.quality(quality);
    ImageResponse response = imageModel.call(new ImagePrompt(prompt, builder.build()));
    List<String> results = new ArrayList<>();
    for (Image image : response.getResults()) {
        String r = image.getB64Json();
        if (r == null || r.isEmpty()) r = image.getUrl();
        if (r != null && !r.isEmpty()) results.add(r);
    }
    if (results.isEmpty()) throw new RuntimeException("图片生成返回结果为空");
    return results;
}
```

- [ ] **步骤 2：generateImage 落库多图**

生成分支改为：

```java
// 纯文生图：多张结果各自落盘
List<String> localPaths = new ArrayList<>();
if (hasReferenceImages || "edit".equals(mode)) {
    // edits 模式维持单图（现有语义）
    result = callImageEdit(effectiveModel, prompt, referenceImages, generatedImageUrl);
    localPaths.add(fileStorageService.saveImageFromBase64(result));
} else {
    for (String r : callOpenAIImage(effectiveModel, prompt, size, quality, aspectRatio, n)) {
        localPaths.add(r.startsWith("http") ? fileStorageService.saveImage(r)
                : fileStorageService.saveImageFromBase64(r));
    }
}
if (scene != null) {
    scene.setImageUrl(localPaths.getFirst());
    scene.setImageUrls(String.join(",", localPaths));
    scene.setImageStatus("completed");
    sceneMapper.updateById(scene);
}
return localPaths.getFirst();
```

`String result` 局部变量重构为 `List<String>`（注意 edits 分支现有 `localPath` 变量删除/复用）。

- [ ] **步骤 3：AIController.generateImage 返回 imageUrls**

```java
@PostMapping("/generate-image")
public ApiResponse<Map<String, String>> generateImage(@RequestBody GenerateImageRequest request) {
    String imageUrl = imageService.generateImage(...);  // 不变
    // 从 scene 读回 image_urls（service 已落库）
    Map<String, String> result = new LinkedHashMap<>();
    result.put("imageUrl", imageUrl);
    result.put("sceneId", request.sceneId());
    return ApiResponse.ok(result);
}
```

（imageUrls 前端通过 loadProject 的 SceneResponse 拿到，无需接口重复返回。）

- [ ] **步骤 4：编译验证**

```bash
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q
```
预期：BUILD SUCCESS。

- [ ] **步骤 5：Commit**

```bash
cd /e/Desktop/AI-storyboard && git add AIStoryboardBackend/src/main/java/com/storyboard/service/ai/impl/ImageGenerationServiceImpl.java AIStoryboardBackend/src/main/java/com/storyboard/controller/AIController.java && git commit -m "feat: 生图 n>1 多图返回并落 image_urls（imageUrl 保留首图兼容）"
```

### 任务 B4：generate-video 参考素材透传

**文件：**
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/dto/request/GenerateVideoRequest.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/service/ai/VideoGenerationService.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/service/ai/impl/VideoGenerationServiceImpl.java`
- 修改：`AIStoryboardBackend/src/main/java/com/storyboard/controller/AIController.java`

- [ ] **步骤 1：GenerateVideoRequest 加字段**

```java
List<String> referenceVideos,
List<String> referenceAudios
```

- [ ] **步骤 2：VideoGenerationService 接口签名扩展**

```java
String createVideoTask(String sceneId, String prompt, String alias,
        String resolution, String size, String aspectRatio,
        Integer duration, String negativePrompt, Long seed,
        List<String> referenceImages, String generatedImageUrl,
        List<String> referenceVideos, List<String> referenceAudios);
```

（保留旧签名为默认重载，供 Agent 链路等调用方零改动：旧签名调用新签名传 null。）

- [ ] **步骤 3：实现——参考素材文件转 data URI / mm_file://**

新增私有方法，在 createVideoTask 组装 body 时调用：

```java
// 参考素材转换：小文件(≤10MB)读本地文件转 data URI 内联；大文件经网关上传代理换 mm_file://
private String resolveReferenceUrl(String urlPath, String type) {
    if (urlPath == null || urlPath.isBlank()) return null;
    if (urlPath.startsWith("data:") || urlPath.startsWith("http") || urlPath.startsWith("mm_file://")) {
        return urlPath;
    }
    String filename = extractFilename(urlPath);
    Path localFile = switch (type) {
        case "video" -> fileStorageService.resolveVideo(filename);
        case "audio" -> fileStorageService.resolveAudio(filename);
        default -> fileStorageService.resolveImage(filename);
    };
    if (!Files.exists(localFile)) {
        log.warn("参考素材文件不存在: {}", localFile);
        return null;
    }
    try {
        byte[] bytes = Files.readAllBytes(localFile);
        if (bytes.length <= 10 * 1024 * 1024) {
            String mime = FileStorageService.contentType(filename);
            return "data:" + mime + ";base64," + Base64.getEncoder().encodeToString(bytes);
        }
        // 大文件：multipart 字节流 → 网关 /v1/files/upload → mm_file://
        MultipartBuilder mp = new MultipartBuilder().file("file", filename, mimeOf(filename, type), bytes);
        byte[] bodyBytes = mp.build();
        HttpRequest req = HttpRequest.newBuilder()
                .uri(URI.create(config.getGatewayBaseUrl() + "/v1/files/upload"))
                .header("Content-Type", "application/octet-stream")
                .header("Authorization", "Bearer " + config.getGatewayApiKey())
                .timeout(Duration.ofSeconds(300))
                .POST(HttpRequest.BodyPublishers.ofByteArray(bodyBytes))
                .build();
        HttpResponse<String> resp = httpClient.send(req, HttpResponse.BodyHandlers.ofString());
        if (resp.statusCode() != 200) {
            throw new RuntimeException("参考素材上传失败: " + resp.statusCode() + " " + resp.body());
        }
        JsonNode root = objectMapper.readTree(resp.body());
        return root.path("file_id").asText("");
    } catch (Exception e) {
        throw new RuntimeException("参考素材转换失败: " + e.getMessage(), e);
    }
}

private String mimeOf(String filename, String type) {
    String ext = filename.substring(filename.lastIndexOf('.') + 1).toLowerCase();
    return switch (ext) {
        case "mp4" -> "video/mp4";
        case "mov" -> "video/quicktime";
        case "wav" -> "audio/wav";
        case "m4a" -> "audio/mp4";
        default -> "audio/mpeg";
    };
}
```

- [ ] **步骤 4：createVideoTask 组装 body 与互斥**

```java
// 参考素材：任一存在 → 多模态参考模式（r2va），不再传首帧 imageUrl
List<String> refImages = new ArrayList<>();
List<String> refVideos = new ArrayList<>();
List<String> refAudios = new ArrayList<>();
if (referenceVideos != null) for (String v : referenceVideos) { String u = resolveReferenceUrl(v, "video"); if (u != null) refVideos.add(u); }
if (referenceAudios != null) for (String a : referenceAudios) { String u = resolveReferenceUrl(a, "audio"); if (u != null) refAudios.add(u); }
if (referenceImages != null) for (String i : referenceImages) { String u = resolveReferenceUrl(i, "image"); if (u != null) refImages.add(u); }
boolean hasRefs = !refImages.isEmpty() || !refVideos.isEmpty() || !refAudios.isEmpty();
if (hasRefs) {
    body.put("referenceImages", refImages);
    body.put("referenceVideos", refVideos);
    body.put("referenceAudios", refAudios);
} else {
    // 原有 i2v 首帧逻辑不变（generatedImageUrl 或 referenceImages 首图）
    if (generatedImageUrl != null && !generatedImageUrl.isEmpty()) { ...原逻辑... }
    else if (referenceImages != null && !referenceImages.isEmpty()) { ...原逻辑... }
}
```

- [ ] **步骤 5：AIController 传新参数**

```java
String taskId = videoService.createVideoTask(
        request.sceneId(), request.prompt(), request.model(),
        request.resolution(), request.size(), request.aspectRatio(),
        request.duration(), request.negativePrompt(), request.seed(),
        request.referenceImages(), request.generatedImageUrl(),
        request.referenceVideos(), request.referenceAudios());
```

- [ ] **步骤 6：编译验证**

```bash
"/e/Development/apache-maven-3.9.15/bin/mvn.cmd" -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\pom.xml" compile -q
```
预期：BUILD SUCCESS。

- [ ] **步骤 7：Commit**

```bash
cd /e/Desktop/AI-storyboard && git add AIStoryboardBackend/src/main/java/com/storyboard/dto/request/GenerateVideoRequest.java AIStoryboardBackend/src/main/java/com/storyboard/service/ai AIStoryboardBackend/src/main/java/com/storyboard/controller/AIController.java && git commit -m "feat: 视频生成参考素材透传——小文件 data URI 内联/大文件经网关上传代理 mm_file://，参考素材与首帧互斥"
```

### 任务 B5：migration 应用 + 冒烟

- [ ] **步骤 1：psql 应用 V6 migration（本地库 newworkflow）**

```bash
PGPASSWORD=123456 psql -h localhost -U postgres -d newworkflow -f "E:\\Desktop\\AI-storyboard\\AIStoryboardBackend\\src\\main\\resources\\db\\migration\\V6__scene_params_and_refs.sql"
```
验证：`PGPASSWORD=123456 psql -h localhost -U postgres -d newworkflow -c "\d scenes" | grep -E "image_urls|video_aspect_ratio"` 有输出。

- [ ] **步骤 2：起 8085 冒烟实例（先停占用 8085 的旧实例）**

```bash
# 停旧实例（如有）：powershell -NoProfile -Command "Get-NetTCPConnection -LocalPort 8085 -State Listen | Select-Object -ExpandProperty OwningProcess | ForEach-Object { Stop-Process -Id $_ -Force }"
cd /e/Desktop/AI-storyboard/AIStoryboardBackend
SERVER_PORT=8085 JAVA_HOME="C:\\Program Files\\Java\\jdk-21" "/e/Development/apache-maven-3.9.15/bin/mvn.cmd" spring-boot:run -q
```
（后台运行，等 "Started" 日志。）

- [ ] **步骤 3：冒烟脚本（curl）**

```bash
# 1) 登录拿 JWT（用本地用户）
# 2) 上传三类参考素材：
curl -s -X POST "http://localhost:8085/api/scenes/{sceneId}/references" -H "Authorization: Bearer $TOKEN" -F "type=image" -F "file=@test.png"
curl -s -X POST "http://localhost:8085/api/scenes/{sceneId}/references" -H "Authorization: Bearer $TOKEN" -F "type=video" -F "file=@test.mp4"
curl -s -X POST "http://localhost:8085/api/scenes/{sceneId}/references" -H "Authorization: Bearer $TOKEN" -F "type=audio" -F "file=@test.mp3"
# 预期：返回 {id,type,url,fileName,fileSize}；超限返回 40001
# 3) 列表：GET /api/scenes/{sceneId}/references → 3 条
# 4) 删除：DELETE /api/scenes/references/{id} → 成功
# 5) 参数覆盖：PUT /api/scenes/{id} {"imageN":4,"videoModel":"MiniMax-H3","videoAspectRatio":"16:9"} → 响应含新字段
```

**阶段 B 完成标志：** migration 应用成功、冒烟实例可上传/列表/删除参考素材、参数覆盖落库。

---

## 阶段 C：前端（AIStoryboardClient）

### 任务 C1：类型与 API

**文件：**
- 修改：`AIStoryboardClient/src/api/projects.ts`
- 修改：`AIStoryboardClient/src/api/scenes.ts`
- 修改：`AIStoryboardClient/src/api/ai.ts`
- 修改：`AIStoryboardClient/src/config.ts`

- [ ] **步骤 1：projects.ts SceneResponse 加字段 + 素材类型**

```ts
export interface SceneReferenceAsset {
  id: string;
  type: 'image' | 'video' | 'audio';
  url: string;
  fileName: string;
  fileSize: number;
}

export interface SceneResponse {
  // ...现有字段
  imageUrls: string;
  imageModel: string;
  imageSize: string;
  imageQuality: string;
  imageN: number;
  videoModel: string;
  videoAspectRatio: string;
  // 注意：imageUrls 逗号分隔字符串，前端拆分为数组
}
```

- [ ] **步骤 2：scenes.ts 加素材端点**

```ts
listReferences: (sceneId: string) =>
  client.get<ApiResponse<SceneReferenceAsset[]>>(`/scenes/${sceneId}/references`),
uploadReference: (sceneId: string, type: string, file: File) => {
  const fd = new FormData();
  fd.append('type', type);
  fd.append('file', file);
  return client.post<ApiResponse<SceneReferenceAsset>>(`/scenes/${sceneId}/references`, fd);
},
deleteReference: (id: string) => client.delete(`/scenes/references/${id}`),
```

- [ ] **步骤 3：ai.ts**

```ts
generateImage 响应类型改：ApiResponse<{ imageUrl: string; imageUrls?: string; sceneId: string }>
generateVideo 请求加：referenceVideos?: string[]; referenceAudios?: string[];
```

- [ ] **步骤 4：config.ts 加静态兜底**

```ts
/** 参考素材上传限制静态兜底（网关 params 未配置时使用；键与网关 params 对齐） */
export const REFERENCE_LIMITS = {
  image: { maxCount: 10, maxSizeMB: 30 },
  video: { maxCount: 3, maxSizeMB: 50 },
  audio: { maxCount: 3, maxSizeMB: 15 },
} as const;
```

### 任务 C2：projectStore 重构

**文件：**
- 修改：`AIStoryboardClient/src/stores/projectStore.ts`

- [ ] **步骤 1：移除 getSceneRefs/setSceneRefs，新增素材与覆盖参数状态**

```ts
// 删除：getSceneRefs / setSceneRefs（soundDesign JSON 方案废弃）
// 新增：
sceneRefs: Record<string, SceneReferenceAsset[]>;   // sceneId -> 参考素材列表
sceneParams: Record<string, { imageModel?: string; imageSize?: string; imageQuality?: string; imageN?: number; videoModel?: string; videoDuration?: number; videoResolution?: string; videoAspectRatio?: string }>;
fetchSceneRefs: (sceneId: string) => Promise<void>;
uploadSceneRef: (sceneId: string, type: string, file: File) => Promise<void>;
deleteSceneRef: (sceneId: string, refId: string) => Promise<void>;
setSceneParams: (sceneId: string, params: Record<string, unknown>) => Promise<void>;  // 写 scenes 表 + 本地合并
clearSceneParams: (sceneId: string) => Promise<void>;  // 恢复全局默认（置 null）
```

实现要点：`fetchSceneRefs` 调 `sceneApi.listReferences`；`uploadSceneRef` 成功后 push 本地列表；`setSceneParams` 调 `sceneApi.update` 并 `updateSceneInStore`；`loadProject` 时清空 sceneRefs/sceneParams 并预拉当前选中分镜素材。

- [ ] **步骤 2：generateImage/generateVideo 使用覆盖参数**

```ts
generateImage: async (sceneId, prompt, model, referenceImages, mode, generatedImageUrl) => {
  const { imageSize, imageQuality, imageN } = get();
  const p = get().sceneParams[sceneId] ?? {};
  const res = await aiApi.generateImage({
    sceneId, prompt,
    model: p.imageModel ?? model,
    referenceImages, mode, generatedImageUrl,
    size: p.imageSize ?? imageSize,
    quality: p.imageQuality ?? imageQuality,
    n: p.imageN ?? imageN,
  });
  ...
}
// generateVideo 同理：duration/resolution/aspectRatio 用 p.videoDuration ?? 全局 preset 解析值
// 参考素材：referenceImages/referenceVideos/referenceAudios 从 get().sceneRefs[sceneId] 按 type 取 url 数组
```

### 任务 C3：SceneCard 瘦身

**文件：**
- 修改：`AIStoryboardClient/src/components/scene/SceneCard.tsx`

- [ ] **步骤 1：删除以下区块**

- 折叠区内「Reference image upload」（refInputRef / 上传 / 缩略图）
- 「Toggle ref-image usage」勾选框（useForImage/useForVideo）
- 底部 Image/Video action buttons（handleGenerateImage / handleGenerateVideo / handleVideoRefineConfirm / handleImageRefineConfirm）
- showImageModal / showVideoModal 状态与 ImageRefineModal / VideoRefineModal 引用
- getSceneRefs / setSceneRefs / imageModel / videoModel 等不再使用的 store 订阅

保留：重命名、删除、剧本摘要、标签、生图/生视频提示词 textarea、Video progress bar（状态查看）、未读红点、动画。状态徽标：在标签行加两个小徽标（🖼️ imageStatus / 🎬 videoStatus 文案：pending=未生成 / generating=生成中 / completed=已完成 / failed=失败）。

- [ ] **步骤 2：删除 ImageRefineModal.tsx / VideoRefineModal.tsx**

```bash
rm "E:/Desktop/AI-storyboard/AIStoryboardClient/src/components/ai/ImageRefineModal.tsx" "E:/Desktop/AI-storyboard/AIStoryboardClient/src/components/ai/VideoRefineModal.tsx"
```
先 grep 确认无其他引用：`grep -rn "ImageRefineModal\|VideoRefineModal" AIStoryboardClient/src --include=*.tsx --include=*.ts`。

### 任务 C4：PreviewPanel 生成工作台

**文件：**
- 新建：`AIStoryboardClient/src/components/editor/ReferenceUploader.tsx`
- 修改：`AIStoryboardClient/src/components/editor/PreviewPanel.tsx`

- [ ] **步骤 1：新建 ReferenceUploader 组件**

Props：`{ type: 'image'|'video'|'audio'; items: SceneReferenceAsset[]; maxCount: number; maxSizeMB: number; onUpload: (file: File) => void; onDelete: (id: string) => void }`。渲染：上传虚线框（accept 按 type）+ 剩余额度提示（`n/maxCount，单文件 ≤ maxSizeMB MB`）+ 缩略列表（图片显示 img，视频/音频显示文件名 + 播放/时长提示 + 删除按钮）。文件选择时前端预检：类型（extension/contentType）、大小 ≤ maxSizeMB、数量 < maxCount，超限 alert 并拒绝。

- [ ] **步骤 2：PreviewPanel 重构（图片 tab）**

结构：
```
[剧本内容块]
[Tab: 🖼️ 图片预览 | 🎬 视频预览]
图片 tab:
  ├─ 结果区：imageUrls 拆数组（无则 [imageUrl]）→ 轮播（当前索引 + ◀▶ 按钮 + 圆点）
  │   每张 contain 展示，点击放大灯箱（复用 ImagePreviewModal）
  │   勾选框（每张）→ 「下载选中(n)」按钮（fetch+blob 批量下载，跳过未勾选）
  ├─ 生成参数区（卡片）：
  │   ☑ 参考图生图 [checkbox]（勾选时展开参考图上传）
  │   ├─ 生图模型 select（imageModelOptions，默认=scene.imageModel ?? 全局 imageModel）
  │   ├─ 尺寸 select（当前模型 params.sizes ?? IMAGE_SIZES）
  │   ├─ 质量 select（params.qualities ?? IMAGE_QUALITIES）
  │   ├─ 生成个数 select（params.n min..max；默认 p.imageN ?? 全局 imageN）
  │   ├─ 参考图上传（ReferenceUploader type=image；maxCount=params.refImages.max ?? 10，maxSizeMB=params.maxImageSizeMB ?? 30）
  │   └─ 按钮：🖼️ 生成图片 | 完善图片（有当前图时启用：以当前图首张为源图 mode=edit）
  └─ 参数修改即 setSceneParams(scene.id, ...)；「恢复全局默认」链接置 null
```

图片 tab 只渲染图片相关控件；**不渲染任何视频参数**。

- [ ] **步骤 3：PreviewPanel 重构（视频 tab）**

```
视频 tab:
  ├─ 结果区：videoUrl 播放器 + 下载
  ├─ 生成参数区（卡片）：
  │   ├─ 视频模型 select（videoModelOptions，默认=scene.videoModel ?? 全局）
  │   ├─ 时长 select（params.durations；默认 p.videoDuration ?? 全局 preset.duration）
  │   ├─ 分辨率 select（params.resolutions ?? [768P, 2K]）
  │   ├─ 画幅 select（params.aspectRatios ?? 静态）
  │   ├─ 参考素材区：
  │   │   参考图 ReferenceUploader（maxCount=params.refImages.max ?? 10 / maxSizeMB=params.maxImageSizeMB ?? 30）
  │   │   参考视频 ReferenceUploader（maxCount=params.refVideos.max ?? 3 / maxSizeMB=params.maxVideoSizeMB ?? 50）
  │   │   参考音频 ReferenceUploader（maxCount=params.audioCount.max ?? 3 / maxSizeMB=params.maxAudioSizeMB ?? 15）
  │   ├─ ☑ 以本分镜图片为首帧（有 imageUrl 时可用；开启时禁用参考素材上传区，反之清首帧）
  │   └─ 按钮：🎬 生成视频
  └─ 视频 tab 只渲染视频相关控件；**不渲染任何图片参数**
```

生成参数组装：`model=p.videoModel ?? 全局 videoModel`；参考素材有任一 → referenceImages/referenceVideos/referenceAudios（url 数组），不传首帧；首帧开关开 → 传 generatedImageUrl=scene.imageUrl，不传参考素材。

- [ ] **步骤 4：参数取值辅助**

预览面板内建 `paramValue(p, sceneKey, globalKey, fallback)`：分镜覆盖 > 全局 > 模型默认 > 静态兜底。模型切换时若当前分镜未覆盖，跟随全局（与现有 LeftSidebar 行为一致）；若已覆盖，只改分镜值。

### 任务 C5：验证与收尾

- [ ] **步骤 1：TypeScript 检查**

```bash
cd /e/Desktop/AI-storyboard/AIStoryboardClient && npx tsc -p tsconfig.app.json --noEmit
```
预期：无错误（注意 tsconfig 为 solution-style，必须 -p tsconfig.app.json）。

- [ ] **步骤 2：构建**

```bash
npm run build
```
预期：vite build 成功。

- [ ] **步骤 3：Commit**

```bash
cd /e/Desktop/AI-storyboard && git add AIStoryboardClient && git commit -m "feat: 分镜列表瘦身+预览面板生成工作台——图片tab多图轮播/勾选下载/参考图生图参数，视频tab参考素材+首帧互斥，参数全局默认+分镜覆盖"
```

**阶段 C 完成标志：** tsc + build 通过。

---

## 阶段 D：端到端测试链路（必须做到位）

### 任务 D1：后端链路冒烟

- [ ] **步骤 1：确认 8085 实例运行 + migration 已应用**
- [ ] **步骤 2：完整冒烟脚本（参考 scripts/ 现有 e2e 脚本风格，写 scripts/scene_refactor_smoke.py）**

覆盖：
1. 登录 → 建项目 → 生成分镜（或直接调 addScene 造数据）
2. 上传 image/video/audio 参考素材 → 列表 → 删除
3. 参数覆盖 PUT（imageN=4 等）→ GET 项目验证字段回读
4. generate-image n=4（网关不可用时桩验证：断言请求体含 size/quality/n；实际调用看网关状态）
5. generate-video 带 referenceVideos/referenceAudios → 断言 body 组装（mock 网关或真实网关）

### 任务 D2：前端手工链路（浏览器）

- [ ] **步骤 1：** 分镜列表不再出现参考图上传与勾选框；提示词编辑正常；状态徽标显示
- [ ] **步骤 2：** 图片 tab：生成 4 张 → 轮播切换 → 勾选 2 张下载成功；参考图生图勾选 → 上传参考图（超限拦截）→ 生成（图改图）
- [ ] **步骤 3：** 视频 tab：时长/分辨率/画幅选项与网关 params 一致；参考素材上传约束展示；首帧开关与参考素材互斥
- [ ] **步骤 4：** 参数覆盖持久化：设置分镜参数 → 刷新页面 → 值保留；「恢复全局默认」生效
- [ ] **步骤 5：** 参考视频真生成一条（MiniMax 密钥可用时）

### 任务 D3：回归

- [ ] **步骤 1：** Agent 对话链路（图生视频 /form/submit、AgentTools.writeScenes）不受影响——scenes 表新列不破坏既有查询，generateImage/generateVideo 旧签名重载兼容
- [ ] **步骤 2：** LeftSidebar 全局生图/生视频设置仍可用（全局默认语义不变）
- [ ] **步骤 3：** 完整 `mvn compile`（两仓库）+ `tsc` + `npm run build` 全绿后收尾

---

## 自检记录

- 规格 §3.1（scenes 新列）→ B1；§3.2（素材表）→ B1/B2；§4.1（网关契约）→ A1；§4.2（上传代理）→ A2；§5.1（上传端点）→ B2；§5.2（多图）→ B3；§5.3（视频透传）→ B4；§6.1（SceneCard 瘦身）→ C3；§6.2（PreviewPanel）→ C4；§6.3（store/api）→ C1/C2；§7（校验互斥）→ A1/B2/C4；§8（测试链路）→ 阶段 D。覆盖完整，无遗漏。
- 类型一致性：`SceneReferenceAsset`（前端）↔ `SceneReferenceResponse`（后端）；`sceneParams` 键名与 SceneRequest 字段一致（imageModel/imageSize/imageQuality/imageN/videoModel/videoAspectRatio）。
- Agent 链路兼容：`VideoGenerationService` 旧签名重载保留；`ImageGenerationService.generateImage` 旧签名不变（内部扩展）。
