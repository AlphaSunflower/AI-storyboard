<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.0</version>
        <relativePath/>
    </parent>
    <groupId>com.storyboard</groupId>
    <artifactId>common-core</artifactId>
    <version>0.1.0</version>
    <name>common-core</name>
    <description>AI Storyboard 微服务公共模块 — 统一返回体/业务异常/全局异常处理/通用工具（无业务逻辑）</description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <!-- 全局异常处理注解（@RestControllerAdvice/@ExceptionHandler/ResponseEntity） -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
        </dependency>
        <!-- ApiResponse @JsonInclude + 异常解析（ObjectMapper/JsonNode） -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <!-- MethodArgumentNotValidException.getBindingResult() 依赖 BindException（spring-context） -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>
        <!-- 日志 -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <!-- ClientAbortException（tomcat 特有）；运行时各服务自带 tomcat，此处仅编译期需要 -->
        <dependency>
            <groupId>org.apache.tomcat.embed</groupId>
            <artifactId>tomcat-embed-core</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
